---
date: 2026-04-30
type: recap
session: S54
cssclass: twk-recap
tags: [recap, codex, delegation, wrapper, slash-command, gpt-5.5, cross-model-boundary, session]
---

# Session 54 Recap — codex-exe 위임 wrapper 정착 + 슬래시 명령 캡슐화

## 한 줄 요약
S51 cross-model 위임 패턴 (Opus 4.7 ≠ 구현자 = GPT-5.5) 을 절차로 정착. wrapper script + 슬래시 명령으로 향후 호출 부담 0화. End-to-end PASS (pong round-trip + workspace-write smoke task + 본체 TBV 4 항목).

## 핵심 결정 (3건)
1. **S51 cmd.exe wrapper → S54 직접 호출** — S51 의 cmd.exe wrapper 는 PowerShell tool 환경 한정 (`Start-Process` 가 .cmd wrapper 직접 실행 불가, TGL #69 동형). git bash 의 codex 는 POSIX 셸 wrapper (`exec node codex.js`) → 직접 호출 가능. 환경 진단 후 패턴 단순화
2. **Watch-mode termination > timeout-only** — codex 0.125.0 known shutdown hang (작업 완료 후 "rollout items not found" + WebSocket cleanup). stderr `^tokens used$` 마커 polling 으로 의미적 완료 즉시 감지 → SIGTERM → SIGKILL escalate. 30s timeout-only 패턴 → 6s 단축
3. **TGL/TCL 등록 X, 코드 캡슐화 O** — S52 P0 였던 "Windows codex CLI 호출 패턴 TGL 신규" 를 슬래시 명령 + wrapper script 로 대체. 룰 (LLM 자율 준수 의존) 보다 코드 (강제 실행) 가 더 강한 강제력. 회피 아니라 더 정직한 해법

## 산출 (3건 신규, 0건 수정)
- **[`scripts/codex_exec.sh`](../../scripts/codex_exec.sh)** ~5.5KB — wrapper. 4원칙: 직접 호출 / stdin file redirect / GNU timeout / watch-mode SIGTERM. Output = stdout.log + stderr.log + .done sentinel + .exit code
- **[`.claude/commands/codex-exe.md`](../../.claude/commands/codex-exe.md)** ~5KB — 슬래시 명령. 위임 게이트 4 signal + prompt 5섹션 (Scope/Spec/Deliverables/Smoke/Anti-Acceptance) + 호출 패턴 + TBV 7항목 + S51→S54 진화 표 + 워크플로우 체크리스트
- **[`tests/test_codex_exe_smoke.py`](../../tests/test_codex_exe_smoke.py)** 426 B — codex 가 직접 작성한 입증자료. round-trip 마커 single assertion. pytest 1/1 PASS

## 검증 (end-to-end)
| 시나리오 | 결과 |
|---------|------|
| pong round-trip (read-only sandbox) | 6초 완료, stdout="pong", semantic_ok=1, .done sentinel ✓ |
| workspace-write smoke task | codex 가 1 파일 생성 + 자가 pytest 1/1 PASS 보고 ✓ |
| TBV-1 파일 존재 | tests/test_codex_exe_smoke.py 426 bytes ✓ |
| TBV-2 파일 내용 | spec §2 본문과 정확히 일치 ✓ |
| TBV-3 scope 격리 | git status --porcelain = 1줄 (다른 파일 0건 수정) ✓ |
| TBV-4 pytest 재실행 (위상군 환경) | 1 passed in 0.02s ✓ |
| anti-acceptance 위반 | 0건 (LLM 호출 X / tems_commit X / settings.local 외 수정 X) ✓ |

## TGL #63 적용 (메타 가치)
종일군의 "확실히 된거야?" 모호한 확인 질문에 텍스트 스키마 ("스크립트 작동함") 대신 ASCII 시퀀스 + 실전 mini task 강제 검증. preflight 가 #63 명시 → 응답에서 "TGL #63 에 따라 텍스트가 아닌 노드/엣지로 입증" 명시. 실전 검증 누락 시 다음 세션 첫 사용 부담을 사전 차단.

## S51 → S54 진화 표
| 단계 | S51 (PowerShell tool) | S54 (Bash tool / git bash) |
|------|----------------------|---------------------------|
| 호출 진입 | PowerShell `Start-Process` | bash 직접 |
| .cmd wrapper 우회 | cmd.exe `/c` 필수 | 불필요 |
| stdin EOF | NUL device or 파일 redirect | 파일 redirect |
| 종료 처리 | task-notification 대기 | watch mode (tokens used → SIGTERM) → 24s 단축 |
| 절차화 | 매번 매뉴얼 4단계 | 슬래시 명령 + wrapper 캡슐화 |

## 다음 회차 (S55) 핵심
- (A) **self_cognition_gate detector 임계 재설계** (S52→S53→S54 P0 3회 이월). false positive 7건 누적 = 단일 토큰 매칭이 기술적 outcome / CSS literal 자축 오분류
- (B) **PR-3a (handover_failure_gate path 외부화)** — canonical v0.5 prerequisite (S52 이월)
- (C) **codex zombie 정리 정책** — 본 세션 누적 14개 (codex 7 + cmd 7). taskkill 자동화 또는 PC 재기동 정책
- (D) **TWK v0.4 Phase 0 T7-T21 재개** — S44 이래 9세션 연속 이월
- (E) **`/codex-exe` 실전 적용 첫 케이스** — S55+ 의 작은 spec 구현으로 절차 효용도 측정

## 미진행 (handover §8)
- L3 wiki 페이지 미작성 — 후보 3건 (Codex_Delegation / Cross_Model_Boundary / Watch_Mode_Termination) 시간 소진. Karpathy 억지 작성 금지 원칙 적용
- codex zombie 정리 — 권한 미보유 (taskkill 거부, "shared host" 보안 룰)
- wrapper 의 disk/메모리 누수 점검 — 단일 호출 검증만, 100회 반복 시 /tmp 누적 cleanup 미정

## 메타-결함
0건. 외부 모델 위임 = 본체 ≠ 구현자 분리 → 자기-방어 동기 구조적 차단. S51 (첫 적용) → S54 (두 번째) 누적 = 메타-결함 3차 재발 cycle (S48/S49/S50) 차단의 구조적 해법 입증.
