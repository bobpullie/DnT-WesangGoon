---
date: 2026-04-22
type: recap
cssclass: twk-recap
tags: [session, recap, tems, phase3, layer1, package-distribution, artgoon]
session: S42
---

# S42 Recap — 2026-04-22

## 핵심 성과

1. **TEMS Layer 1 강화 (위상군 로컬)** — preflight 포맷 재설계. `get_rule_health()` 로 `rule_health` 의 `violation_count / compliance_count / fire_count` 를 각 규칙 옆에 `(v:N c:M)` 주석으로 노출. TGL 섹션 헤더 "필수 준수 — 위반 시 violation_count 자동 증가" 로 승격. 말미에 준수 명시 directive (`"TGL #N 에 따라..."`) 삽입. 자연어 주입이 소프트한 한계 속에서 social pressure + explicit acknowledgment 유도.

2. **memory/README.md 캐노니컬 문서 459줄** — 위상군 로컬. 13 섹션 (TL;DR · 왜 필요 · 핵심 개념 · 아키텍처 · 라이프사이클 · 강제력 4계층 · 조용한TEMS · 진화 · 등록 · 배포 · 분리 · 진단 · 이력).

3. **bobpullie/TEMS Phase 3 포팅 + 패키지 배포 (S42 핵심)** — v0.2.0 릴리즈:
   - templates/ 8종 신규 (tool_gate_hook / compliance_tracker / tool_failure_hook / retrospective_hook / pattern_detector / memory_bridge / decay / sdc_commit)
   - templates/preflight_hook.py 에 Layer 1 포팅
   - scaffold.py 확장: copy_templates 10종 / register_hook 6 이벤트 / rule_health Phase 3 컬럼 8개 / FTS5 summary / Phase 2→3 ALTER 마이그레이션
   - pytest 35/35 통과

4. **아트군 실설치 + defect 발견 + v0.2.1 즉시 패치** — `E:/DnT/DnT_ArtGoon` 에 pip 설치 + scaffold. 기존 3 hook 자동 보존 확인. `project:DnT` 태그 규칙이 Registry 없이 필터 drop 되는 결함 live 발견 → template preflight 의 `detect_project_scope` 에 cwd fallback + case-insensitive 매칭 + `__version__` 동기화 + QMD Dense Fallback README 섹션 추가 → v0.2.1 push + tag.

## 주요 결정

| 결정 | 근거 |
|------|------|
| bobpullie/TEMS 를 canonical upstream 으로 확정 | 기존 패키지가 Phase 2 멈춰있음 → Phase 3 포팅해 타 에이전트 `pip install -U + tems scaffold` 로 일괄 이식 가능화 |
| 강제력 4-계층 정립 (L1 자연어 / L2 deny / L3 compliance / L4 DVC) | 사용자 "자연어 TGL 무시" 질문 → 오버헤드 4축 분석으로 Layer 1+2 실무 채택 (Haiku pre-eval 은 50~150s 누적 레이턴시로 제외) |
| cwd fallback + case-insensitive project 태그 | 아트군 실설치 중 결함 발견. Registry 없는 solo agent 가 `project:X` 태그 쓸 수 있도록 |
| QMD Dense Fallback README 섹션 복원 | 사용자 지적 — 코드는 0.1.0 부터 있었는데 README 축약에서 빠짐. "간편 adoption" 명목 부적절 |
| 구 `tems/` 디렉토리 보존 (아트군) | TGL #91 준수. 신 `memory/` 병행 운영, 구 데이터 마이그레이션은 별도 태스크로 이관 |
| Layer 2 테스트 중 self-block 이벤트 기록 | cleanup 명령이 dummy rule 에 막힌 것 → tool_gate_hook 의 live 동작 증명 |

## 발견한 defect (v0.2.0 → v0.2.1 패치)

- `templates/preflight_hook.py:detect_project_scope(agent_id)` 가 `REGISTRY_PATH is None` 시 `["project:meta", "project:all", ""]` 만 반환 → `project:DnT` 태그 규칙이 filter_by_project 에서 drop
- `filter_by_project` 의 case-sensitive 비교 — `project:DnT` vs `project:dnt` 부정 매칭
- `__version__` 상수가 pyproject.toml 버전 bump 와 무관하게 `0.1.0` 하드코딩 상태

## 신규 wiki 페이지 (작성 예정)

- `docs/wiki/decisions/2026-04-22_tems-upstream-canonical.md`
- `docs/wiki/patterns/Enforcement_4_Layer.md`

## 규칙 피드백 (TEMS 등록 후보)

1. (TCL) TEMS 배포/업그레이드 canonical 경로: `pip install -U git+https://github.com/bobpullie/TEMS.git` + `tems scaffold`
2. (TGL-W) 에이전트에 TEMS scaffold 전 `.claude/settings.local.json` 백업 (`.pre-tems-backup`)
3. (TGL-P) template preflight 는 Registry 없으면 cwd fallback. 프로젝트 태그 비교는 case-insensitive

## 다음 세션 (S43) 부트

- HEAD (위상군): S42 종료 commit
- HEAD (bobpullie/TEMS): `7373fb5` v0.2.1
- 관찰 대상: v0.2.1 실설치 사용성, 타 에이전트 Wave 2 재스캐폴드 권장, SDC gate 실전 발동
- 신규 wiki 2페이지 작성 필요 (decisions + patterns)
