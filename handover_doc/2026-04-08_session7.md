---
date: 2026-04-08
type: handover
cssclass: twk-handover
tags: [session, handover]
session: S7
---

# Session 7 — KH Phase 1 수동 검증 부트스트랩

> 2026-04-08 / 위상군 / 종일군 직접 검증 지원

## 세션 목적
종일군의 KH Phase 1 수동 검증(Task 26 Step 3-5) 시작 — Obsidian 설치 / vault 오픈 / 노트 작성 워크플로우 구축 / 그래프 시각화 메커니즘 정비.

## 진행
1. **부트 확인**: `27519fe` + 75 passed 정상.
2. **Obsidian 설치**: `winget install Obsidian.Obsidian` → 1.12.7 설치 성공.
3. **vault 구조 설명**: `notes/`가 inbox, `projects/{DnT_Game,DnT_Fermion,Shared}.md`가 랜딩 페이지(Dataview `FROM "notes"` 쿼리). 루트에 자동생성된 빈 `무제.md`/`무제.canvas` 정리 안내.
4. **Templater 도입** (커뮤니티 플러그인 필요):
   - `notes/_templates/note.md` 생성. 기능: 파일명 `YYYY-MM-DD_` 자동 prefix, project/type/agent_owner 드롭다운 (suggester), summary prompt, frontmatter + 본문 골격 자동 작성.
   - `projects/_template_project.md` 생성. 새 프로젝트 랜딩 페이지 자동 생성용 (이름/진행자/도메인 prompt + Dataview 쿼리 골격 포함).
   - 종일군 설정: Settings → Templater → Template folder=`notes/_templates`, Trigger on new file=ON, Folder templates 2개 등록 (`notes` → note.md, `projects` → _template_project.md).
5. **하드코딩 제거** (종일군 지시):
   - 초안 템플릿은 프로젝트 목록을 `["DnT_Game","DnT_Fermion","Shared"]`로 하드코딩 → 결함.
   - `app.vault.getMarkdownFiles().filter(f => f.path.startsWith("projects/") && !f.basename.startsWith("_"))`로 동적 스캔 변경. 새 랜딩 페이지를 만들면 즉시 드롭다운에 반영.
6. **그래프 엣지 메커니즘**:
   - 문제: Obsidian Graph View는 `[[wikilink]]`만 엣지로 인식. frontmatter 평문은 무시 → 모든 노드 고립.
   - 옵션 A(frontmatter 자체를 link로): Dataview 쿼리 전부 깨짐 → 검증 단계에서 비용 큼.
   - 옵션 B(본문 하단 `**Links:** [[project]] · [[date]] · #type #owner` 자동 삽입): Dataview 보호 + 그래프 엣지 생성 + 두 축(프로젝트/날짜) 동시. **B 채택.**
   - note 템플릿 v3에 반영. 날짜 노드는 unresolved ghost로 시작 (Filter "Existing files only" 해제 시 보임).

## 핵심 결정
| 결정 | 근거 | 위험 |
|------|------|------|
| Templater (Obsidian 커뮤니티 플러그인) 의존 | suggester/prompt/file rename 필요 — 코어 Templates로는 불가능 | 플러그인 의존 추가, 그러나 Obsidian 표준에 가까움 |
| 동적 폴더 스캔으로 드롭다운 생성 | 하드코딩 회피 (종일군 지시), 새 프로젝트 추가 시 1곳 변경만 | `_`로 시작하는 파일은 템플릿/메타로 간주하는 컨벤션 도입 |
| 그래프 엣지는 본문 link 라인으로 우회 | Phase 1 검증 중 Dataview 스키마 깨면 Step 4 재작업 비용 | frontmatter와 본문 정보 약한 중복 — Phase 2에서 통합 예정 |

## 정직하게 짚은 결함 (종일군에게 보고)
1. **새 프로젝트 추가가 3곳 동기화 필요** (랜딩 / Templater 드롭다운 / `khub/cli.py --project` 기본값) → 단일 source of truth 부재. Phase 2 task로 격상 (`KH-P2-project-registry`).
2. **frontmatter ↔ 본문 link 라인 중복**: 동일 정보(project/date) 두 곳 표기. 옵션 B의 의도된 트레이드오프이지만 Phase 2에서 frontmatter 표준을 wikilink 형식으로 마이그레이션해야 정리됨.

## 핸드오버 갱신 (`KH-P2-project-registry` task에 sub-bullet 추가)
> Phase 2에서 frontmatter `project`/`created`를 plain string에서 wikilink (`"[[DnT_Game]]"`, `"[[2026-04-08]]"`) 형식으로 일괄 마이그레이션. 모든 Dataview 쿼리를 link 비교 문법(`= [[X]]`)으로 갱신. 본문 `**Links:**` 라인 우회는 그 시점에 제거.

## 신규 산출물
- `E:/KnowledgeHub/notes/_templates/note.md` (Templater, v3 — 동적 스캔 + 본문 link 라인)
- `E:/KnowledgeHub/projects/_template_project.md` (Templater)
- 핸드오버 `CURRENT_STATE.md`: Session 7로 갱신, Step 3 부분진행 표시, `KH-P2-project-registry` task에 sub-bullet 추가

## Task 26 Step 진행 현황
- ✅ Step 1, ✅ Step 2
- 🟡 **Step 3 (Graph View 클러스터)** — 메커니즘은 구축. 종일군이 노트 몇 개 만들고 그래프에서 클러스터 시각 확인하면 완료
- ⏳ **Step 4 (Dataview 10개)** — 다음 액션
- ⏳ **Step 5 (1주일 사용 판정)** — 진행 중

## 다음 세션 부트
```
작업 디렉토리: E:/KnowledgeHub
git HEAD     : 27519fe (코드 변경 없음 — vault 자산만 추가)
test 명령    : python -m pytest -q
기대 결과    : 75 passed
```

## 다음 세션 TODO (상위 5)
1. 종일군 Step 3-4 결과 수신 → 결함 task화 또는 통과 처리
2. **Step 4 Dataview 10개 렌더링 검증 지원** (에러 수신 시 쿼리 수정)
3. KH-girok-polish 착수
4. MAINT-1 CLAUDE.md TEMS 경로 정정
5. Phase 1 통과 시 → KH 스펙 §17 Phase 2 로드맵 brainstorm 세션 분리 (TCL #64)

## 종일군 액션 체크리스트
- [ ] (필요 시) Obsidian Templater 플러그인 설치/활성화 + Folder templates 2개 등록
- [ ] `notes/`에 노트 2-3개 만들어 드롭다운/날짜 prefix 동작 확인 (이미 1회 확인됨)
- [ ] Graph View 옵션 조정: "Existing files only" 해제, "Tags" 표시 ON
- [ ] 그래프에서 프로젝트/날짜 클러스터링 확인 → Step 3 ✅
- [ ] `projects/DnT_Game.md` Reading view에서 Dataview 표 정상 렌더링 확인 (3개 랜딩 합쳐 쿼리 10개) → Step 4 ✅
- [ ] 1주일 일상 사용 → Step 5 판정
