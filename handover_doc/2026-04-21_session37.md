# Session 37 — 2026-04-21 (위상군, DnT)

## 세션 개요

S37 세션. 주제: **SDC-Helper CLI 구현 + bobpullie/handover 독립 스킬 신설 및 배포**.

S36 종료 시 P0으로 지정된 SDC-Helper(sdc_commit.py) 구현을 완료하고, 추가 요청으로 위상군의 핸드오버 시스템 전체를 독립 GitHub 리포(`bobpullie/handover`)로 패키징·배포하였다.

## 논의 흐름 및 결정

### 1. SDC-Helper 구현 (P0 — TCL #120)

**배경:** S36에서 SDC Auto-Dispatch Gate(`check_sdc_gate`)가 Phase 3 tool_gate_hook에 편입됨. git 쓰기 명령 직전 `sdc_brief_submitted=false`이면 `<sdc-gate-alert>` 경고가 주입되는 구조인데, brief 제출을 선언하려면 JSON을 직접 편집해야 했음 — 이 노이즈를 제거하는 CLI helper.

**구현 (Sonnet 위임 → trust-but-verify):**
- `memory/sdc_commit.py` (175 LOC): `--verdict {KEEP,DELEGATE,STAGING}` / `--task` / `--rationale` / `--brief` / `--reset` / `--json`
- `active_guards.json.sdc_brief_submitted=true` 세팅 + `memory/sdc_briefs.jsonl` audit log
- `memory/tests/test_sdc_commit.py` (135 LOC): 5 tests
- `memory/tool_gate_hook.py` SELF_INVOCATION_MARKERS에 `memory/sdc_commit.py` 추가
- `.gitignore`: `memory/sdc_briefs.jsonl` 추가
- pytest 10/10 PASS (test_sdc_commit 5 + test_sdc_gate 5)
- Commit: `4058e2a`

**사용법:**
```bash
python memory/sdc_commit.py --verdict KEEP --task "S38-xxx" --rationale "Q2 KEEP: ..."
# → sdc_brief_submitted=true, gate 해제
# git commit/push 후 reset은 자동 (다음 세션 시작 시 false)
```

### 2. bobpullie/handover 독립 스킬 신설

**요청:** "다른 미니PC나 새 에이전트가 다운받아 바로 사용할 수 있는 핸드오버 시스템"

**브레인스토밍 (4 Q&A):**
- Q1 의존성: C (코어 독립 + adapter 분리) → 실질 A (새 에이전트 즉시 사용)
- Q2 설치: C (setup script + Stop hook 자동화)
- Q3 Stop hook 범위: A (Skeleton — 기계적 섹션 자동, TODO placeholder)
- Q4 구조: B (TWK 패턴 — 디렉토리 구조)

**구현 (Sonnet 위임 STAGING → 본체 commit/push):**
```
bobpullie/handover/
├── SKILL.md          # Boot/Shutdown Protocol
├── scripts/
│   ├── setup.py      # SessionStart+Stop hook 자동 등록, --migrate 플래그
│   └── stop_hook.py  # git log/status 포함 CURRENT_STATE.md.draft 자동 생성
├── templates/        # CURRENT_STATE.md + session-doc.md
└── adapters/
    ├── triad.json          # TEMS+QMD+TWK 연동 단계
    └── triad-minimal.json  # QMD만
```

**--migrate 플래그 (본체 직접 추가):**
- `settings.json` + `settings.local.json` 양쪽에서 기존 핸드오버 hook 탐지
- OLD/NEW 명령어 diff 출력 → y/N 확인 → 제거 후 새 hook 등록
- 취소 시 변경 없음

**Commits:** `c374201` (initial 10 files 602 LOC) + `aa7d56c` (--migrate +119 LOC)
**배포:** `https://github.com/bobpullie/handover` — 공개 접근 가능

## 코드 변경 요약

| 레포 | 파일 | 변경 |
|------|------|------|
| DnT-WesangGoon | `memory/sdc_commit.py` | 신설 175 LOC |
| DnT-WesangGoon | `memory/tests/test_sdc_commit.py` | 신설 135 LOC |
| DnT-WesangGoon | `memory/tool_gate_hook.py` | +1줄 SELF_INVOCATION_MARKERS |
| DnT-WesangGoon | `.gitignore` | +1줄 sdc_briefs.jsonl |
| bobpullie/handover | 전체 | 신설 10 files 721 LOC |

## 미완료 사항

- 위상군 자체에 handover 스킬 적용 검증 (`--migrate` 실제 실행) 미수행
- S37 원래 P0 SDC-Gate-Observation (false positive 패턴, pull/fetch trigger) 미착수
- TWK wiki: S36에서 예정한 SDC gate decision + postmortem 미작성

## 다음 세션 TODO

- [ ] **SDC-Gate-Observation (P0)** — false positive 빈도 관찰, `pull`/`fetch`가 gate 트리거 포함 여부 판단. sdc_brief_submitted 세션 리셋 자동화 여부 결정.
- [ ] **handover 스킬 위상군 적용** — `--migrate` 실행해서 settings.local.json 기존 hook → 표준 hook 교체 검증
- [ ] **Push-decision-other (P1)** — 코드군(52f8dff 미푸시) / 디니군(S34 미커밋) / 리얼군(S34 migration 미커밋) 푸시 여부 판단
- [ ] **TWK wiki** — SDC gate Phase 3 편입 decision + postmortem (S36 예정분 이월)
- [ ] **NeedsReview-Classification (P1)** — 위상군 22건 + 코드군 14건 수동 재분류
- [ ] **QMD-Embed-115-121 (P2)** — 신규 규칙 #115~#121 embed
