# Session 33 — Phase 3 재검증 + Migration + DVC skill + 코드군 TEMS 이식 + 커밋

> 날짜: 2026-04-20
> HEAD 시작 (위상군): 4c9b468 (S27~S32 전부 미커밋)
> HEAD 종료 (위상군): **eec891e** — S31-S33 통합 커밋, 미푸시
> HEAD 종료 (코드군): **52f8dff** — Wave 1 이식 커밋, 미푸시 (기존 38 commits ahead + 1 TEMS)

## 핵심 논의

1. **종일군 지시: "TEMS audit 요청 및 검증 재실행"** → S32 말미 audit P0/P1 4건 패치의 독립 재검증 필요
2. 이후 **"즉시패치"** → P1-a-follow (had_violation 보존) + stale guard eviction 바로 적용
3. **TCL/TGL 분류 방식 질의** → 3축 판단 프레임 (일반화 가능성, 추상화 수준, 매칭 메커니즘) 정립
4. **기존 규칙 재분류 상태 확인** → 79/81 NULL 판정 → **"실행하라"**로 Migration-Classification 승인
5. **"다른 agent에 TEMS 적용"** 요청 → 4개 agent 정찰 후 Wave 1/2 분리 제안
6. **코드군 먼저 상태점검** → S29 표준화(2026-04-13) 고정 + `tems` 패키지 미설치로 1개월간 무력화 판명
7. **원조 TCL 정체성 발견** → 코드군 `src/checklist/` = 결정론적 빌드 검증 체크리스트 (TEMS TCL과 다른 시스템)
8. **DVC 네이밍 결정** → Deterministic Verification Checklist. 종일군 결정(1 DVC / 2 docs/BUILD_VERIFICATION.md 위치 / 3 실제 매커니즘+템플릿 / 4 TEMS와 병행)
9. **코드군 TEMS Wave 1 이식** + CLAUDE.md 편집 한 차례 거부 → 승인 후 편집
10. **"커밋하라"** → 위상군 + 코드군 각각 커밋

## 결정 사항

### P1-a-follow + stale eviction 즉시 패치
- had_violation=True guard는 remaining_checks 리셋 금지 (tool_gate + preflight 양쪽)
- .update() 대체해 had_violation 덮어쓰기 방지
- 24h TTL stale guard eviction (observation-only 세션 누적 방지)

### DVC skill 구조
- 디렉토리 기반 skill (`.claude/skills/dvc/`) + templates/ 폴더에 실제 작동 코드
- init.py 스캐폴딩 CLI 제공 (dogfood 검증 통과)
- 위상군에 먼저 설치 → 1~2세션 실사용 후 글로벌 승격 판단

### 코드군 Wave 1 범위
- Phase 0-2 + Migration-Classification 적용
- Phase 3 (tool_gate, compliance_tracker) 제외 — 관찰 기간 후 Wave 2
- self-contained 모드 (외부 tems 패키지 의존 제거, TGL #92 준수)
- 기존 `save_session_end.py`는 유지 + `retrospective_hook` 병행 추가

### 용어 분리 (DVC vs TEMS TCL)
- **DVC case** = `src/checklist/cases.json` + `chk_*.py`. `DISPLAY_HUMANIZE_001` 형식
- **TEMS TCL** = `memory/error_logs.db` 자연어 규칙. `#N` 형식
- 혼동 방지: "DVC case #ID" vs "TEMS TCL #N" 접두사 의무 사용

## 코드 변경

### 위상군 (eec891e)

#### TEMS 패치
- [memory/tool_gate_hook.py:159-203](memory/tool_gate_hook.py#L159-L203) — `record_active_guard`에 had_violation 보존 + 조건부 remaining_checks 리셋 (P1-a-follow)
- [memory/preflight_hook.py:211-238](memory/preflight_hook.py#L211-L238) — `_push_active_guards`에서 `.update()` 제거, 슬롯 개별 보충
- [memory/preflight_hook.py:545](memory/preflight_hook.py#L545) — 하드코딩 경로 `E:/AgentInterface/...` → 상대 경로 `memory/tems_commit.py`
- [memory/compliance_tracker.py:16-21](memory/compliance_tracker.py#L16-L21) — `STALE_GUARD_TTL = timedelta(hours=24)` 상수
- [memory/compliance_tracker.py:278-306](memory/compliance_tracker.py#L278-L306) — main() 진입부 stale eviction 루프 + `log_event("eviction", ...)`

#### Migration-Classification
- [memory/_migrate_classification.py](memory/_migrate_classification.py) 신규 — auto_classify_tgl + abstraction 휴리스틱 + needs_review 플래그. `--apply` / dry-run / limit 지원
- 31개 TGL 규칙 classification + abstraction_level backfill. 22 needs_review, 11 high-confidence

#### DVC skill 신규
- [.claude/skills/dvc/SKILL.md](.claude/skills/dvc/SKILL.md) — frontmatter + 3 시나리오 + TEMS 경계 선언
- [.claude/skills/dvc/ARCHITECTURE.md](.claude/skills/dvc/ARCHITECTURE.md) — 설계 원리 10가지 + 실행 흐름 + 한계
- [.claude/skills/dvc/GUIDE.md](.claude/skills/dvc/GUIDE.md) — 스캐폴딩 + case 추가 + 바이패스 + CI 통합
- [.claude/skills/dvc/init.py](.claude/skills/dvc/init.py) — 스캐폴딩 CLI (smoke test 통과)
- [.claude/skills/dvc/templates/](.claude/skills/dvc/templates/) — base.py (dvc_config.json 지원), runner.py (auto-discovery), chk_example.py, cases.json, __init__.py

#### DVC dogfood (위상군)
- [src/checklist/](src/checklist/) — init.py로 스캐폴드. 5 파일
- [dvc_config.json](dvc_config.json) — 기본 경로 설정
- [docs/BUILD_VERIFICATION.md](docs/BUILD_VERIFICATION.md) — DVC ≠ TEMS TCL 선언 + 사용법

#### TEMS 규칙 등록 (이번 세션)
- **TCL #107**: P1-a-follow + stale eviction 패치 기록
- **TCL #108**: Migration-Classification 실행 기록 (31개 backfill)
- **TCL #109**: DVC skill 생성 기록
- **TCL #110**: DVC 위상군 dogfood 설치 기록
- **TCL #111**: 코드군 TEMS Wave 1 이식 기록

### 코드군 (52f8dff)

#### 이식 파일 10종 (self-contained)
`memory/{__init__, fts5_memory, tems_engine, tems_commit, preflight_hook, backfill_triggers, tool_failure_hook, pattern_detector, retrospective_hook, decay, _migrate_classification}.py`

#### DB 스키마 ALTER
rule_health에 8컬럼 추가: `fire_count, last_fired, compliance_count, violation_count, classification, abstraction_level, needs_review, created_at`. 기존 47개 memory_logs 전부 rule_health row backfill.

#### Migration-Classification 적용
14/14 TGL backfill — TGL-M 13, TGL-T 1, L2 12, L1 2, 14 needs_review.

#### Hook 배선 확장
- `.claude/settings.local.json`: PostToolUse Bash → `tool_failure_hook`, Stop → `retrospective_hook` 병행 추가

#### 문서
- `CLAUDE.md` v2026.4 — TCL/TGL 재정의 + DVC ≠ TEMS TCL 선언
- `docs/BUILD_VERIFICATION.md` 신규 — DVC 정체성 + 15 case 카탈로그

#### 백업
- `memory/_pre_TEMS_Wave1_20260420/` (gitignored) — rollback 포인트

## TEMS 규칙 변경

- **TCL/TGL 누적:** #1~#111. archive: #64, #98
- **이번 세션 신규:** #107 (P1a-follow), #108 (Migration), #109 (DVC skill), #110 (DVC 위상군), #111 (코드군 Wave 1)
- **Migration 결과:** TGL 33개 classification 분포 — TGL-M 21, TGL-T 3, TGL-P 3, TGL-S 2, TGL-C 2, TGL-W 1, TGL-D 1. needs_review 22/33

## 검증

### P1-a-follow smoke test (4 case all passed)
1. tool_gate 재발동 + had_violation=True → remaining_checks=1 유지 ✓
2. tool_gate 재발동 + had_violation 없음 → remaining_checks=5 정상 리셋 ✓
3. preflight 재발동 + had_violation=True → remaining_checks=2 유지 ✓
4. compliance_tracker stale guard (30h old) → 정상 evict ✓

### DVC smoke test (init.py)
- 7 파일 스캐폴드 정상
- auto-discovery: example 모듈 등록 ✓
- 3 case 실행: pass/fail/suggestion 출력 정상
- console/json 포맷 작동

### 코드군 TEMS smoke test
- 10 모듈 clean import ✓
- preflight_hook: TCL 힌트 출력 + #3/#34 주입 ✓
- decay.py: 47 rules, 0 transitions (정상) ✓
- tems_commit.py: 새 CLI 옵션 (--classification, --forbidden 등) 작동 ✓
- tool_failure_hook, retrospective_hook: stdin smoke pass ✓

### audit 재검증 (code-reviewer agent 독립)
- P0 (FORBIDDEN fallback): **Resolved** (경미 우려)
- P1-a (dedup): **Partially Resolved** → P1-a-follow 후속 패치로 완결
- P1-b (backslash): **Resolved**
- P1-c (scope-aware): **Resolved** (엣지 홀)
- 최종: Ship-with-followups → 즉시패치 완료 후 Ship-clean

## 미완료 / 다음 세션 TODO (S34)

### P0
- **위상군 + 코드군 push** — 종일군 명시 지시 대기. 위상군 `eec891e` (origin/master + 1), 코드군 `52f8dff` (origin/main + 39)
- **코드군 needs_review 14건 수동 재분류** — 코드군과 직접 진행 (위상군 관여 X, 종일군 확정)

### P1
- **Phase 3 Wave 2 배포 판단** — 1~2세션 위상군 Phase 3 관찰 후 코드군에 tool_gate + compliance_tracker 추가
- **위상군 needs_review 22건 수동 재분류** — TGL-M fallback 16개 포함
- **다른 agent TEMS 확장** (리얼군/디니군/어플군) — Wave 1 모델 준비됨
- **Phase3-P2-Tests** — memory/tests/ 단위 테스트 3종 (test_tool_gate_hook, test_compliance_tracker, test_decay)
- **Phase3-P2-CreatedAt** — compliance_tracker.update_counts 에서 INSERT OR IGNORE + UPDATE 분리
- **Phase3-Decay-Cron** — Windows Task Scheduler 매일 09:00 decay.py 등록
- **Phase3-QMD-Embed** — #102~#111 신규 규칙 qmd embed (tems-wesanggoon 컬렉션)

### P2
- **DVC 글로벌 승격 판단** — 위상군 1~2세션 실사용 후 C:/Users/bluei/.claude/skills/ 로 승격 여부
- **Phase3-P2-ProtocolDoc** — .claude/rules/tems-protocol.md 에 FORBIDDEN fallback 조건 문서화
- **audit follow-ups** — MCP 도구 classification, active_guards.json 동시성 잠금, extract_forbidden_text 라인 앵커
- **TEMS-패키지커밋** — bobpullie/TEMS 별도 repo 업데이트분 커밋 (별도 트랙)

## 위험 / 주의사항

- **위상군 Phase 3 실운영 관찰 기간 부족** — 약 1일 (S32~S33). Wave 2 확산 전 코드군 Wave 1 관찰도 병행 필요
- **코드군 needs_review 14/14 전건** — auto_classify 신뢰도 낮음. 실제 카테고리는 수동 확인 필수
- **39 commits ahead of origin (코드군)** — push 시점 판단 필요. 일부 dart_cache 는 unrelated session 작업
- **위상군 push하면 미니PC/서버 동기화 필요** — 종일군 직접 결정
- **코드군 CLAUDE.md 1회 편집 거부** — cross-project 수정 조심. 다음 세션에서도 명시 승인 없이 편집 금지
- **DVC `# chk:skip` 바이패스** — 사유 필수 규약 재강조 필요. 감사(`--audit-bypass`) 20건 초과 시 경고

## 부트 정보 (S34)

```
작업 디렉토리: E:\DnT\DnT_WesangGoon (주), E:\QuantProject\DnT_Fermion (코드군 TEMS)
HEAD (위상군): eec891e — S31-S33 통합 커밋, 미푸시
HEAD (코드군): 52f8dff — Wave 1 TEMS 커밋, 미푸시 (origin/main + 39)
HEAD (wesangAgent 독립): 03d6638 (변경 없음)
HEAD (TEMS bobpullie): 변경 없음 (S27 이월분 잔존)

위상군 TEMS: self-contained + Phase 0-3 완료 + Migration 완료 + P1a-follow/stale-eviction 패치
  - 로컬 모듈 14종 (Phase 0-3 + decay + Migration 유틸)
  - 규칙: #1~#111 (archive: #64, #98)
  - 자동등록 모드: ON (TCL #97)
  - 스냅샷(gitignored): _post_S29_standardization, _post_S32_phase3_preP0_patch, _post_S33_P1a_follow_patch

코드군 TEMS: Wave 1 (Phase 0-2) 배포 완료
  - 로컬 모듈 11종
  - 규칙 47개 (TCL 33 + TGL 14)
  - Migration 완료, needs_review 14건 대기
  - Hook: SessionStart(custom) / UserPromptSubmit(preflight) / PostToolUse Write|Edit(memory_bridge) / PostToolUse Bash(tool_failure) / Stop(save_session + retrospective 병행)
  - 스냅샷(gitignored): _pre_TEMS_Wave1_20260420/

DVC 시스템:
  - Skill: .claude/skills/dvc/ (SKILL/ARCHITECTURE/GUIDE/init + templates)
  - 위상군 dogfood: src/checklist/ 설치됨
  - 코드군 원본: src/checklist/ 15 case (유지됨)

상태: 종일군 지시 대기 (push 여부, Wave 2 일정, 다른 agent 확장)
```

## S34 Task (우선순위)

| ID | 우선순위 | 내용 |
|----|---------|------|
| **Push-decision** | P0 | 위상군 + 코드군 push 여부 판단 |
| **Code-goon-NeedsReview** | P0 | 코드군 14건 수동 재분류 (코드군과 직접) |
| **Phase3-Observation** | P1 | 위상군 Phase 3 + 코드군 Wave 1 관찰 1~2 세션 |
| **WesangGoon-NeedsReview** | P1 | 위상군 22건 수동 재분류 |
| **Phase3-Wave2-Decision** | P1 | 코드군 tool_gate + compliance_tracker 배포 판단 |
| **Phase3-Decay-Cron** | P1 | Windows Task Scheduler 매일 09:00 등록 |
| **Phase3-QMD-Embed** | P1 | #102~#111 qmd embed |
| **Other-agents-TEMS** | P1 | 리얼군/디니군/어플군 Wave 1 확장 |
| **DVC-Global-Promotion** | P2 | 1~2세션 후 글로벌 승격 판단 |
| **Phase3-P2-Tests** | P2 | 단위 테스트 3종 (tool_gate/compliance_tracker/decay) |
| **Phase3-P2-CreatedAt** | P2 | compliance_tracker INSERT OR IGNORE 분리 |
| **Phase3-P2-ProtocolDoc** | P3 | tems-protocol.md FORBIDDEN fallback 문서화 |
| **TEMS-패키지커밋** | P3 | bobpullie/TEMS 업데이트분 커밋 |
