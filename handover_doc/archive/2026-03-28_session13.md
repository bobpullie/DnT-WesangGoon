# Handover — Session 13 (2026-03-28)

## 세션 요약
밴드→과녁 전환 빌드 완료 (ADR-002-R1 + ADR-010). 7단계 빌드 + 기획군 검토 + 3건 수정 + 커밋/푸시.

---

## 완료 사항

### 1. 미커밋 상태 확인 → 이미 완료
- MRV S11+S12 변경사항은 이전 세션에서 이미 커밋+푸시 완료 확인 (0568bf4, b6f79fc)
- AgentInterface는 git 레포 아님

### 2. 밴드→과녁 빌드 (7단계)

#### Step 1: Shared 타입/상수 변경
- `types/score.ts`: SharpnessInfo 인터페이스 제거, ATRConfig에서 minRatio/maxRatio 제거, ScoreBreakdown에서 sharpnessMultiplier → normalizedDistance, 알고리즘 주석 v3으로 갱신
- `types/prediction.ts`: PredictionInput에 `targetPrice: number` 필드 추가
- `constants/game.ts`: SCORING에서 BAND_HIT_SCORE/MISS/SHARPNESS 제거 → TARGET_RADIUS_ATR(1.5)/TARGET_EXPONENT(1.5) 추가. ATR_CONFIG에서 MIN_RATIO/MAX_RATIO 제거. BAND_LIMITS 완전 삭제 → ITEM_SLOT 상수 추가 (BAR_HEIGHT:48, SLOT_SIZE:40, HIT_AREA:44, SLOT_COUNT:6, HOLD_MS:150, FINGER_OFFSET_PX:40, UNPLACED_TIMEOUT_SEC:30)

#### Step 2: 서버 채점 로직 변경
- `scoring.service.ts`: calcBandScore → ATR 정규화 거리 곡선 `100×(1-nD/1.5)^1.5`. calcSharpnessMultiplier 완전 삭제. 불스아이 판정 0.25 ATR. scorePrediction에서 targetPrice fallback 처리 (band 중심)
- `game-engine.routes.ts`: BAND_LIMITS import → ATR_CONFIG. 요청에서 targetPrice 수신, 서버에서 band 자동 계산 (targetPrice ± ATR×0.75). 밴드폭 검증 로직 제거
- `round.service.ts`: sharpnessMultiplier → normalizedDistance (DB 컬럼 재활용)
- `live-band-score.ts`: estimateLiveBandScore 시그니처 변경 (4인자→3인자: currentPrice, targetPrice, currentATR). sharpness 로직 제거
- `user-round.manager.test.ts`: 모든 PredictionInput/Prediction에 targetPrice 추가, sharpness 통합 테스트 → 거리 기반 테스트로 교체

#### Step 3: gameStore 상태 변경
- `selectedBand: Band|null` → `targetPrice: number|null`
- `setSelectedBand` → `setTargetPrice`
- `submittedTargetPrice: number|null` 추가
- `markPredictionSubmitted` 시그니처 변경: (lastKlineOpenTime, currentATR) → 내부에서 submittedBand 자동 계산
- `previousResult` 타입에 targetPrice 추가
- Band, DisplayRoundState, SendMessageFn 타입 export

#### Step 4: ItemSlotBar 컴포넌트 (신규)
- `modules/item-slot/ItemSlotBar.tsx`: 하단 48px fixed, 6슬롯 (M1: 과녁만 활성), SUBMIT 버튼 우측
- 150ms 홀드 → 리프트 → onSlotDragStart 콜백
- 잠긴 슬롯 탭 → onLockedSlotTap 콜백
- visible prop으로 슬라이드 업/다운 (0.2초 transition)

#### Step 5: TargetDragger 컴포넌트 (신규)
- `modules/item-slot/TargetDragger.tsx`: forwardRef + useImperativeHandle로 startDragFromSlot 외부 노출
- 슬롯→차트 드래그: lifting(10px 상향 감지) → dragging(coordinateToPrice 역변환)
- 과녁 재배치: 30px 반경 히트 영역, stopPropagation
- 드래그 프리뷰: 40px 점선 원 + 가격 레이블
- 미배치 30초 만료: useEffect 타이머 → currentPrice 자동 배치 + onAutoSubmit
- 멀티터치 취소: activePointerIdRef로 첫 포인터 추적, 2번째 pointerdown 시 cancelDrag

#### Step 6: PredictionTarget mode 확장
- `PredictionTarget.tsx`: props 변경 (bandLow/bandHigh → targetPrice+currentATR)
- mode prop: 'placed'(골드, 풀 opacity) | 'locked'(시안, 맥동) | 'result'(골드, 종가 마커)
- 4링 동심원: bullseye(0.25ATR) / inner(0.5ATR) / mid(1.0ATR) / outer(1.5ATR)
- ATR 기반 가격→픽셀 변환으로 반경 계산 (줌 레벨 반응)
- result 모드: 종가 마커(빨간 점) + 거리 선(점선)

#### Step 7: GameScreen 통합
- PredictionBandSelector 삭제 (파일 + import + 사용처)
- PREDICTING 사이드 패널 제거 (D102)
- ItemSlotBar 추가 (visible=isPredicting && !predictionSubmitted)
- TargetDragger ref 연결 (targetDraggerRef → handleSlotDragStart)
- PredictionTarget 호출 변경 (targetPrice, currentATR, mode)
- LiveBandGauge 호출 변경 (targetPrice 기반)
- sharpnessMultiplier 계산 블록 완전 제거
- handleSubmit: targetPrice 전송, markPredictionSubmitted에 currentATR 전달
- OnboardingScreen: PredictionBandSelector 제거, 간이 클릭 배치로 대체

### 3. 기획군 검토 실행 → CONDITIONAL PASS
- **CRITICAL**: TargetDragger ref 미연결 → forwardRef+useImperativeHandle로 수정
- **HIGH**: 미배치 30초 만료 미구현 → useEffect 타이머 추가
- **HIGH**: 멀티터치 드래그 취소 미구현 → activePointerIdRef + 2번째 pointerdown 감지
- MEDIUM: 드래그 프리뷰 rAF 미사용 (향후 개선)
- LOW: 슬롯 펄스 애니메이션 미구현, preview 모드 미분리

### 4. 커밋 + 푸시
- `23b0896` feat: 밴드→과녁 전환 빌드 (ADR-002-R1 + ADR-010) — 22 files, +1178 -870
- v3 브랜치 origin 푸시 완료

### 5. 추가 변경
- `LivePredictionGauge.tsx`: band 기반 → targetPrice+ATR 거리 기반
- `ScoreAnimation.tsx`: narrowBandBonus → Bullseye HIT 표시
- `prediction/index.ts`: PredictionBandSelector export 제거

---

## 미커밋 상태
- **MRV**: 없음 (전부 커밋+푸시 완료)
- **AgentInterface**: 핸드오버 + QMD 세션 기록

---

## 다음 세션 TODO

### 핵심
1. **서버 pull + 빌드 확인** — `cd E:\MRV\src && npm run build:shared && npm run dev`
2. **Turn 1 TEST/POSTMORTEM** — 과녁 빌드 실제 플레이 테스트
   - 슬롯→드래그 배치 동작 확인
   - 과녁 4링 시각 확인
   - 채점 결과 검증 (거리 기반 곡선)
   - 모바일 터치 테스트 (가능하면)
3. **기획군 검토 LOW 항목 처리** — 슬롯 펄스 애니메이션, preview 모드, rAF 프리뷰

### 후속
4. **CCS Phase 1** — Theatre.js Studio
5. **궤적 시스템** — 5분 모드 + Candle Call 완료 후
6. **AVIS Gemini 빌링** — Phase 2-3

---

## 핵심 결정 기록
| 결정 | 근거 | 결정자 |
|------|------|--------|
| DB 컬럼 재활용 (sharpnessMultiplier → normalizedDistance) | 마이그레이션 없이 기존 컬럼 활용 | 위상군 |
| 불스아이 판정 0.25 ATR | ADR-002-R1 동심원 구조와 일치 | 위상군 |
| OnboardingScreen 간이 클릭 배치로 대체 | 튜토리얼 UX는 향후 별도 기획 필요 | 위상군 |
| forwardRef 패턴으로 ref 배선 | 기획군 CRITICAL 피드백 반영 | 기획군→위상군 |

---

*위상군 | Session 13 | 2026-03-28*
