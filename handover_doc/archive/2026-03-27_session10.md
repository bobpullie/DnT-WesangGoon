# Handover — Session 10 (2026-03-27)

## 세션 요약
S9 미커밋 푸시 → CCS(Chart Control System) 설계 → Phase 0 빌드 → 종일군 테스트 → 세로축 버그 2건 수정 → 프리셋 JSON 2개 수령. CCS의 핵심 Scan/Apply 루프가 동작 확인됨.

---

## 완료 사항

### 1. S9 미커밋 Push
- 커밋 `fc39239` — 49파일 +9,987줄 (ADR-007 + AVIS + 궤적 기획 + DB 마이그레이션 + 파라미터 레지스트리)

### 2. CCS (Chart Control System) 설계 — ADR-008
- **종일군 제안 분석:** 차트인터페이스 + 차트스캐너 개념을 위상군이 정리/보완
- **외부 도구 조사:** Theatre.js (1순위, GUI 키프레임 에디터), GSAP, anime.js 등 비교
- **ADR-008 작성:** `docs/decisions/ADR-008_chart_control_system.md`
  - 3-Layer 아키텍처: ChartCamera (추상 좌표계) + Scanner + Interface
  - Phase 0: 정지모드 MVP (Theatre.js 없이)
  - Phase 1: Theatre.js Studio 통합 (트래킹모드)
  - Phase 2: 프로덕션 연출 엔진
- **확정 결정:**
  - Dev 진입: `?dev=true` → 텔레그램 이식 시 `/dev` 라우트 전환
  - 세로 마진 읽기: priceToCoordinate 역산 방식 채택
  - Theatre.js Studio 도입 확정 (Phase 1)

### 3. CCS Phase 0 빌드
- **빌드 지시서:** `docs/tasks/BUILD_CCS_phase0.md`
- **빌드군 실행:** Task 1~7 전체 완료
- **신규 6파일:** chart-camera.ts(타입), CameraStateTracker.ts, chartCamera.ts(Scanner), chartInterface.ts(Interface), DevToolbar.tsx, ScannerPanel.tsx, CameraDisplay.tsx
- **수정 5파일:** CandlestickChart.tsx, TargetZoom.ts, GameScreen.tsx, shared/types/index.ts, chart/index.ts
- **커밋:** `a6afa8a` — 16파일 +1,608줄

### 4. 세로축 버그 2건 수정

#### 버그 A: scaleMargins가 캔들 데이터에 의존
- **문제:** Scan과 Apply 시점의 캔들이 다르면 같은 scaleMargins가 다른 화면을 만듦
- **수정:** ChartCameraState에 `priceHigh/priceLow` 추가. Apply 시 `computeApplyMargins()`로 역산

#### 버그 B: scaleMargins는 autoScale:true일 때만 작동
- **문제:** `autoScale:false` + `scaleMargins` 동시 설정 → 세로축 변경 미반영
- **수정:** (1) `autoScale:true` + `scaleMargins` 적용 → (2) 2프레임 후 `autoScale:false` 전환
- **TGL#48 등록**

### 5. 종일군 프리셋 JSON 수령
- `tools/avis/screenshots/Predicting_and_Live.json` — L:40 R:16 Top:0.088 Bot:0.286
- `tools/avis/screenshots/Result.json` — L:29 R:25 Top:-0.064 Bot:0.299

### 6. PROTECTED_CODE 갱신
- `CHART_ALIGN.PAST_MULTIPLIER`: 30.0 → 5.0 (S9 변경 반영)

---

## 미커밋 상태
- MRV: 클린 (커밋+푸시 완료)

---

## 다음 세션 TODO

### 핵심 (CCS Phase 0 → 프리셋 통합)
1. **프리셋 레지스트리 구현** — 종일군 JSON을 코드 내 상수로 저장
2. **기존 하드코딩 대체** — `alignToTimeframe`/`useTargetZoom`을 프리셋 기반 `applyCameraState()`로 점진 전환
   - alignToTimeframe → `applyCameraState(predicting_live 프리셋, animate:true)`
   - useTargetZoom 줌인 → 같은 프리셋 (PREDICTING과 LIVE가 같은 프리셋)
   - useTargetZoom 줌아웃 → autoScale:true 복원 (BROWSING 기본)
   - RESULT → `applyCameraState(result 프리셋)`
   - **주의:** useTargetZoom의 유저 상호작용 감지/줌 범위 저장/복원 로직은 보존해야 함

### 후속
3. **CCS Phase 1** — Theatre.js Studio 통합 (트래킹모드, 키프레임 시퀀스)
4. **궤적 시스템** — 종일군 승인 후 ADR-009 설계
5. **Candle Call Phase 1** — saveRoundResult에 candleCallTotal 저장, candle_calls DB 영속화
6. **Turn 1 TEST/POSTMORTEM** — 전체 코어 루프 테스트
7. **AVIS Gemini 빌링** — Phase 2-3 자동 검증 루프 가동

---

## 핵심 결정 기록
| 결정 | 근거 | 결정자 |
|------|------|--------|
| CCS 3-Layer 아키텍처 | 에이전트-종일군 차트 소통 비용 근본 제거 | 위상군 설계, 종일군 승인 |
| Theatre.js Studio 도입 (Phase 1) | GUI 키프레임 에디터 + JSON export, Apache 2.0 | 위상군 조사, 종일군 승인 |
| `?dev=true` → 텔레그램 시 `/dev` | 개발 단계에서는 쿼리파라미터가 편리 | 종일군 결정 |
| priceToCoordinate 역산 방식 | Shadow만으로는 유저 수동 조작 시 부정확 | 위상군 제안, 종일군 승인 |
| scaleMargins 2단계 적용 | autoScale:true일 때만 scaleMargins 작동 | TGL#48 (실패에서 발견) |
| 프리셋 통합은 다음 세션으로 | 기존 상태머신 대체는 충분한 테스트 시간 필요 | 위상군 제안, 종일군 동의 |

---

## TGL 신규 등록
- **TGL#48:** scaleMargins는 autoScale:true일 때만 효과. false이면 무시됨. 세로축 변경 시 (1) autoScale:true + scaleMargins → (2) 2프레임 후 autoScale:false. 필수 2단계.

---

*위상군 | Session 10 | 2026-03-27*
