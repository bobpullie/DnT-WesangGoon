# 위상군 핸드오버 — 2026-04-06 Session 4

## 세션 요약
Corvis (구 ClawWorker) Docker 에이전트의 SOUL.md 미로딩 문제 진단 및 해결.

## 수행 작업

### 1. OpenClaw 최신 버전 웹 조사
- 공식 문서 7개 소스에서 구조/설정법 조사
- 부트스트랩 파일 주입 메커니즘 확인: workspace 디렉토리의 SOUL.md, AGENTS.md, IDENTITY.md, USER.md 등이 매 턴마다 시스템 프롬프트에 자동 주입됨
- 핵심 발견: `agents.defaults.workspace` 설정이 필수

### 2. OpenClaw 가이드 문서 작성
- `E:/02_openclawAgent/docs/OPENCLAW_GUIDE.md` 생성
- 디렉토리 구조, 부트스트랩 파일 목록, openclaw.json 스키마, Docker 배포 방법 정리

### 3. 설정 수정 (1차 — 실패)
- `openclaw.json`에 `identity`, `agent` (단수) 키 추가 → **스키마 검증 실패**
- 에러: `Unrecognized keys: "identity", "agent"` → 컨테이너 크래시 루프

### 4. 설정 수정 (2차 — 성공)
- `openclaw config schema` 명령으로 실제 스키마 확인 (37개 루트 키)
- `agents.defaults.workspace: "/workspace"` 추가 (핵심 수정)
- `identity`, `agent` 키 제거, 기존 `agents.defaults` 구조 유지
- 컨테이너 정상 기동, 텔레그램 봇 시작 확인

### 5. 에이전트 페르소나 파일 정비
- SOUL.md: 4167B → 1705B (59% 축소, 중복 제거)
- AGENTS.md: 8줄 → 30줄 (운영 규칙 보강)
- IDENTITY.md: 신규 (이름: Corvis, 이모지: 🦞)
- USER.md: 신규 (종일군 프로필)

### 6. 세션 데이터 초기화
- Docker 볼륨 `02_openclawagent_openclaw-state` 삭제 (이전 "코비스" 세션 제거)
- 종일군 확인: 텔레그램에서 Corvis 정상 응답

### 7. Git 커밋
- `0ca1bec` — 전체 변경사항 커밋

## 핵심 결정
| 결정 | 근거 | 결정자 |
|------|------|--------|
| agents.defaults 구조 유지 | OpenClaw 2026.4.2 스키마에 identity/agent 키 없음 | 위상군 |
| 에이전트명 Corvis | 종일군이 이전 세션에서 명명 | 종일군 |
| 세션 볼륨 삭제 | "코비스" 정체성 세션 데이터 제거 필요 | 종일군 승인 |

## 종일군 질문/답변 기록
1. **git 로컬 레포로 되돌릴 수 있는가?** → 예, 커밋 단위로 가능. 원격은 백업/협업용
2. **MCP 서버는 앱 실행 필요한가?** → 예, MCP는 앱 내부 브릿지. 앱 꺼져있으면 연결 실패
3. **Ollama 실행 시 GPU 자동 로드?** → 아니오, 요청 시에만 로드. 5분 미사용 시 자동 언로드
4. **공유 GPU 메모리란?** → 시스템 RAM을 GPU가 빌려쓰는 영역. VRAM 1/40 속도
5. **GPU 4.3GB 사용 중인 이유?** → Windows GUI 앱들 (Chrome, VS Code, Epic 등) GPU 가속

## 미해결/대기
- Task 9: MCP 서버 연결 (후디니/언리얼) — 미착수
- Corvis 통합 테스트 (자율 모드, 태스크 실행) — 미착수
- SOUL.md 토큰 최적화 — 완료 (59% 축소)

## 다음 세션 TODO
1. Task 9: 후디니/언리얼 MCP 서버 연결 테스트
2. Corvis 자율 모드 통합 테스트
3. Q-002: SidePanel crossfade 검증 (P0)
