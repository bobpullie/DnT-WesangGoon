---
date: 2026-04-09
type: handover
cssclass: twk-handover
tags: [session, handover]
session: S11
---

# Session 11 Handover — 2026-04-09

## 세션 목표
TEMS + QMD Dense Vector Upgrade Tier 1 구현 (스펙 §5 Tier 1, S1~S2 범위)

## 수행 내역

### 세션 부트
- STATE.md, CHANGELOG.md, QUEUE.md 없음 (MRV .workflow/ 미생성)
- CURRENT_STATE.md에서 S10 컨텍스트 복원
- 종일군이 설계 스펙 `docs/superpowers/specs/2026-04-09-tems-qmd-upgrade-design.md` 제시

### Tier 1 플랜 작성
- `writing-plans` 스킬로 7개 Task 플랜 작성
- 저장: `docs/superpowers/plans/2026-04-09-tems-qmd-tier1.md`

### Tier 1 구현 (subagent-driven-development)
| Task | 내용 | 결과 |
|------|------|------|
| **1** | QMD 컬렉션 생성 + per-rule 내보내기 | `sync_rules_to_qmd()` → 45개 개별 .md 파일, stale 정리 |
| **2** | `_dense_search()` 컬렉션 교정 | `wesanggoon-sessions` → `tems-wesanggoon`, rule_id 추출+DB 로드 |
| **3** | `tems_commit` 자동 QMD 동기화 | `sync_single_rule_to_qmd()` 신규, 커밋 즉시 .md 생성 |
| **4** | BM25 miss → Dense catch 비교 | 10개 쿼리 A/B, Dense 9/10 BM25 miss 보완 |
| **5** | Preflight 시맨틱 폴백 | BM25 < 2 hits → HybridRetriever 자동 폴백 |
| **6** | RRF 가중치 재튜닝 | sparse 0.35~0.85, dense 0.15~0.65 |
| **7** | 전체 동기화 + 최종 검증 | 45규칙 embed, 6 unit tests PASS, E2E 검증 OK |

### 현장 발견 + 수정 (플랜에 없던 것)
1. **QMD 가상 경로 `_` → `-` 변환**: `rule_0068.md` → `rule-0068.md`. `_extract_rule_id()` 양쪽 지원 추가
2. **QMD stdout 프리앰블**: 쿼리 확장 텍스트가 JSON 앞에 출력. `stdout.find("[")` 으로 JSON 추출
3. **Windows `qmd` → `qmd.cmd`**: npm 글로벌 바이너리는 Python subprocess에서 `.cmd` 필요. `QMD_CMD` 상수 도입
4. **`qmd query` CUDA 리랭커 크래시**: `qmd vsearch` (vector-only)로 대체
5. **QMD 컬렉션 생성 CLI 동작**: `qmd collection add <name> <path>` 가 아닌 CWD 기반 → `add` + `rename` 조합

## 수정된 파일
- `memory/tems_engine.py` — HybridRetriever 전면 리팩터 (dense_search, sync, RRF, QMD_CMD)
- `memory/tems_commit.py` — 자동 QMD 동기화 + sys.path 설정
- `memory/preflight_hook.py` — 시맨틱 폴백 분기 (1-b단계)

## 생성된 파일
- `memory/qmd_rules/rule_*.md` — 45개 규칙 벡터 인덱싱용 파일
- `memory/tests/test_qmd_sync.py` — 5개 유닛 테스트
- `memory/tests/test_preflight_semantic.py` — 1개 유닛 테스트
- `memory/tests/test_hybrid_quality.py` — 품질 비교 스크립트
- `docs/superpowers/plans/2026-04-09-tems-qmd-tier1.md` — 구현 플랜

## 핵심 결정
| 결정 | 근거 |
|------|------|
| `qmd vsearch` 사용 (query 대신) | CUDA 리랭커 크래시, vsearch는 정상 |
| `QMD_CMD` Windows 분기 | Python subprocess FileNotFoundError |
| JSON `[` 시작점 추출 | QMD stdout에 프리앰블 포함 |
| 하이픈+언더스코어 양쪽 지원 | QMD 가상 경로 변환 예측 불가 |
| RRF sparse 0.35~0.85, dense 0.15~0.65 | 규칙 DB는 keyword_trigger 잘 설계되어 BM25 기본 우위 |

## 검증 결과
- 6개 유닛 테스트 전체 PASS
- 품질 비교: 10개 쿼리 중 Dense가 9개 BM25 miss 보완, 0개 miss
- Preflight E2E: 시맨틱 쿼리에서 관련 규칙 정상 주입

## 다음 세션 TODO
### Tier 2 진행 (S3~S5)
- S3: `tems_registry.json` 구현 + `/TEMS:project` 글로벌 스킬 코어
- S4: `/TEMS:project` 확장 (add, rename, retire, reactivate)
- S5: 기존 10개 에이전트 일괄 온보딩

### 부트 명령
```bash
# 테스트 확인
cd "E:/DnT/DnT_WesangGoon" && python -m memory.tests.test_qmd_sync && python -m memory.tests.test_preflight_semantic

# QMD 컬렉션 확인
qmd collection show tems-wesanggoon

# 품질 비교 (optional)
python -m memory.tests.test_hybrid_quality
```

### 미해결 이슈
- `qmd query` CUDA 리랭커 크래시 → 가우스군에게 확인 요청 (VRAM 경합?)
- git repo 미초기화 — 커밋 없음 (종일군 판단 필요)
