# Session 32 — TEMS Phase 3 (Enforcement) 구현 + P0/P1 audit 패치

> 날짜: 2026-04-19 → 2026-04-20 (세션 연속)
> HEAD 시작: 4c9b468 (S31 변경분 미커밋)
> HEAD 종료: 4c9b468 (S31+S32 변경분 전부 미커밋)
> 작업 디렉토리: `E:\DnT\DnT_WesangGoon`

## 핵심 논의

종일군 지시: **"위상군 이전세션에 이어 TEMS작업 재개"** → S31에서 정의된 S32 P0 태스크 3종 (Phase3-PreToolUse, Phase3-Compliance, Phase3-Decay) 실행.
이후 종일군 지시: **"audit 리뷰 및 검증"** → code-reviewer agent 독립 감사.
이후 종일군 지시: **"P0/P1 즉시 패치 + 다음 세션 audit 재검증"**.

## 결정 사항

1. **PreToolUse hook 채택**: matcher="" (모든 도구). TGL-T + `tool_pattern` 슬롯 기반.
   - severity=critical → JSON `permissionDecision: "deny"` 차단
   - severity=info/warning → `<tgl-tool-alert>` 경고만
2. **Compliance tracking**: `active_guards.json` 상태 파일을 PreToolUse↔PostToolUse 간 공유. preflight가 TGL 발동 시 push, compliance_tracker가 window 소비.
3. **Decay 규칙**: 30일→cold, 90일→archive. `effective_last_activity`는 `last_fired > last_activated > memory_logs.timestamp > status_changed_at`. `rule_health.created_at`은 백필 시점이라 제외.
4. **Self-invocation 범위 축소**: Bash 명령어가 hook 자체를 실행할 때만. Edit/Write file_path가 memory/*.py인 경우는 gate 대상으로 유지.
5. **audit P0/P1 4건 즉시 패치** (P2는 S33 이월).

## 코드 변경

### 신규 파일
- [memory/tool_gate_hook.py](memory/tool_gate_hook.py) (282줄) — PreToolUse gate
- [memory/compliance_tracker.py](memory/compliance_tracker.py) (~330줄) — PostToolUse compliance
- [memory/decay.py](memory/decay.py) (~180줄) — 30/90일 자동 전환 CLI

### 수정 파일
- [memory/preflight_hook.py](memory/preflight_hook.py) — `record_fire` 확장, `_push_active_guards` 신규 함수
- [.claude/settings.local.json](.claude/settings.local.json) — PreToolUse matcher="" + PostToolUse matcher="" 추가
- [handover_doc/CURRENT_STATE.md](handover_doc/CURRENT_STATE.md) — S32 성과 + S33 부트 블록 + S33 태스크

### 스냅샷 폴더
- [memory/_post_S32_phase3_preP0_patch/](memory/_post_S32_phase3_preP0_patch/) — P0/P1 패치 직전 4개 파일 (rollback 포인트)

### P0/P1 패치 상세

| ID | 위치 | 변경 |
|----|------|------|
| P0 | `compliance_tracker.check_violation` | FORBIDDEN 키워드 휴리스틱을 fallback으로 한정. `tool_pattern` + `failure_signature` 둘 다 없고 `MUTATING_TOOLS`(Edit/Write/Bash/NotebookEdit)일 때만 동작. distinct 토큰 dedup + `FORBIDDEN_NOISE_TOKENS`에 memory/hook/preflight/compliance 등 인프라 명사 확장. 3 distinct hits 요구 (이전 3 occurrences). |
| P1-a | `tool_gate_hook.record_active_guard` | rule_id 기준 dedup. 이미 active 시 window 리셋 + 슬롯 보충만. |
| P1-b | `build_match_target` (양쪽 hook) | `\\` → `/` 정규화. Windows backslash 경로도 forward-slash 정규식 한 가지로 매칭. |
| P1-c | `compliance_tracker.main` | scope-aware `remaining_checks` 감소. 관찰형 도구(Read/Glob/Grep/WebFetch 등)는 guard window를 건드리지 않고 통과. 변형 도구만 tick 소비 + compliance 적립 대상. |

## TEMS 규칙 변경

- **TGL #102 등록** — TGL-T/L2 시범. `tool_pattern`: `(Edit|Write).*memory[\/](preflight_hook|...)\.py`. Phase 3 게이트 실제 작동 검증용.
- **TCL #103 등록** — Phase 3 구현 완료 기록.
- **TCL #104 등록** — decay.py cron 운영 정책.
- **TCL #105 등록** — P0/P1 패치 완료 기록.
- **#102 카운터 리셋** — audit 진행 중 FORBIDDEN false-positive로 누적된 violation_count를 0으로 초기화. 패치 후 clean-start.

## 검증

### E2E 사이클 (패치 후)
- `echo '{"prompt":"memory preflight_hook self-modify"}' | python memory/preflight_hook.py` → TGL #102 주입 + active_guards push
- Read `memory/preflight_hook.py` → **violation 0** (P0 검증 — 이전 false-positive 해소)
- Edit `memory\DnT\...\memory\tool_failure_hook.py` (backslash) → **violation 1** (P1-b 검증)
- tool_gate 동시 발동 → **guard 1개 유지** (P1-a 검증 — dedup)
- Read 5회 후 `remaining_checks` 변화 **없음** (P1-c 검증 — scope-aware)
- Bash 8회 clean cycle → **compliance=1** (정상 적립)

### 감사 결과 (code-reviewer agent)
- 판정: **Ship-with-fixes**
- 발견: P0 1건 + P1 3건 + P2 3건
- P0/P1 4건은 본 세션에서 즉시 패치 완료, P2 3건은 S33 이월

### 단위 테스트
- **0건** — `memory/tests/`에 test_tool_gate_hook.py, test_compliance_tracker.py, test_decay.py 미작성. S33 P1 태스크로 이월.

## 미완료 / 다음 세션 TODO

### S33 P0
- **Phase3-Audit-Reverify** — code-reviewer agent 재호출. P0/P1 패치가 실제로 감사 지적을 해소했는지 독립 재검증.

### S33 P1 (audit 후 우선순위 조정 가능)
- **Phase3-P2-Tests** — memory/tests/ 3종 단위 테스트 추가
- **Phase3-P2-CreatedAt** — `compliance_tracker.update_counts`에서 `INSERT OR IGNORE` + `UPDATE` 분리로 `created_at` 오염 방지
- **Phase3-Observation** — Phase 3 운영 1~2 세션 관찰, `compliance_events.jsonl` 분석
- **Phase3-Decay-Cron** — Windows Task Scheduler 등록
- **Phase3-QMD-Embed** — #102~#105 신규 규칙 qmd embed
- **Migration-Classification** — 기존 65+개 규칙 classification/abstraction 자동 채우기

### S33 P2
- **Phase3-P2-ProtocolDoc** — `.claude/rules/tems-protocol.md`에 FORBIDDEN fallback 조건 + MUTATING_TOOLS 개념 문서화
- **Other-agents-rollback** — 디니/기록/빌드 등 Phase 0~3 적용 (TCL #93 정책 준수, 종일군 승인 후)

## 위험 / 주의사항

- **미커밋 스코프 거대함** — S27~S32 누적 미커밋 분이 많음. handover_doc/ 신규 세션 파일 3~4개, memory/ 신규 파일 7개, qmd_rules/ 10개, 스냅샷 폴더 2개. 커밋 시점은 종일군 지시 대기.
- **#102 tool_pattern은 tool_gate에서 severity=info로 경고만** — 실제 차단을 원하면 severity=critical 규칙으로 별도 등록 필요. 현 세션은 측정·기록만 목적.
- **compliance_tracker는 모든 PostToolUse에 발동** — 성능 부하 관측 필요. 현재 SQLite 로컬 + rule_health <100행 범위에서는 무시 가능. 규칙 확장 시 캐시 또는 matcher 축소 검토.
- **FORBIDDEN fallback 휴리스틱은 여전히 오탐 여지** — P0 패치로 대폭 줄였으나 완전 제거 아님. 운영 관찰 후 재조정.

## 부트 정보 (S33)

```
작업 디렉토리: E:\DnT\DnT_WesangGoon
HEAD: 4c9b468
규칙: TCL/TGL #1~#105 (archive: #64, #98)
활성 hook: 6개 (SessionStart, UserPromptSubmit, PreToolUse, PostToolUse×3, Stop)
테스트 수: 0 (단위) / E2E smoke 4건 (이 세션 검증)
```
