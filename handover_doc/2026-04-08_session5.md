# Session 5 — 2026-04-08 (위상군)

## 목표
KH-003 Phase 3 (Task 15-21) 구현 — subagent-driven-development 모드.

## 결과
✅ **완료.** HEAD `28f87e7` → `d488cb6`. 7 commits. Tests **54 → 69 passed**.

## 커밋
| Task | 내용 | SHA |
|------|------|-----|
| 15 | CLI 기본 구조 + /k-capture (idea/task/testrun/session/feedback) | `3146ad1` |
| 16 | /k-journal + /k-search | `b365338` |
| 17 | /k-link + /k-index + /k-graph + /k-girok stub | `d761cf8` |
| 18 | `khub/girok.py` GirokAgent (on_session_end, sync_external_sources, friction_review, daily_check) | `08317d3` |
| 19 | `.agents/girok-goon.md` subagent 정의 | `c06ba93` |
| 20 | `khub/visualizer.py` (DATAVIEW_QUERIES Q1-Q10 + Mermaid) | `430db4c` |
| 21 | `projects/{DnT_Game,DnT_Fermion,Shared}.md` 랜딩 페이지 | `d488cb6` |

## 워크플로우
- **subagent-driven-development** 스킬 사용. 각 task: implementer subagent → 통합 code-reviewer subagent → 다음 task.
- 통합 리뷰어 사이클 유지 (Session 3-4 결정 계승, 2단계 spec/quality 분리 대신).
- Task 19+21은 마크다운 전용이라 병렬 dispatch (선형 history 유지됨).
- 최종 통합 리뷰까지 dispatch (Phase 3 holistic review).

## 핵심 결정/검증
- **TCL #65 준수**: `girok.py`가 `with self._conn()` 대신 `try/finally close` 패턴 사용. `get_connection()`은 raw `sqlite3.Connection` 반환 (ctx mgr 아님) 확인. Task 18 implementer가 자발적으로 plan의 `with` 패턴을 try/finally로 교정.
- **TGL #9 준수**: 모든 CLI 테스트가 `click.testing.CliRunner` 사용 (subprocess 회피).
- **WikiAPI 시그니처 일치**: `add_node(type, frontmatter, body)`, `add_edge(source, target, relation)`, `get_neighbors(node_id, depth=)`, `get_project_timeline(project, since=)` 모두 plan과 일치.
- **`record_friction` 시그니처**: `(conn, pattern_text, pattern_type, journal_node_id=None)` — Task 16/18 호출 호환.
- **Edge 객체 attribute**: `e.source_id`, `e.target_id`, `e.relation.value` (Task 20 visualizer 검증).
- **claude_standard 어댑터 fixture**: `# Title` 헤딩 + `## 세션 요약` 블록 필요. Task 18 fixture가 이를 충족하여 비-trivial 검증.

## 백로그 (Phase 4 우선순위)
**Important (다음 세션 첫 처리)**:
1. **`/k-girok` ↔ `GirokAgent` 미연결** — `cli.py`의 `girok_cmd`가 stub만 출력, GirokAgent를 호출하지 않음. Phase 4에서 `/k-girok daily | sync | friction` 서브커맨드 wiring 필요.
2. **`agent_role` 분류 헬퍼 중앙화** — `girok.py:on_session_end`가 `"director" if "wesang" in agent_owner else "specialist"` 하드코드. `cli.capture_session`은 이 필드 자체를 안 씀 → 일관성 깨짐. `khub/roles.py` 또는 `schema.py` 상수 맵으로 추출.
3. **Path 헬퍼 중앙화** — `cli.get_db_path()`와 `girok.GirokAgent.__init__`이 `.index/wiki.db` 경로를 각자 구성. `khub/paths.py` 통합.

**Minor**:
4. `index --source` flag 미구현 — 구현 또는 제거.
5. `index --full` 시 `edges` 테이블 미삭제 — orphan edge 위험.
6. `journal --count/--duration-min/--resolved-by` 옵션 미사용 — `record_friction`이 1회/호출 증가만 함.
7. `generate_session_mermaid` 양방향 walk 시 edge 중복 가능 — `(source, target, relation)` dedup set 추가.
8. KH-orphan: Task 10 재싱크 시 node_id 재생성 orphan 경로 (Phase 2 backlog 잔존).

## Phase 3 통합 리뷰 평가
- **PASS.** 4-layer stack 정합: human entry (CLI) → automation (girok) → presentation (visualizer + landing pages) → data (Phase 1-2).
- 모듈 사이즈 건강: cli.py 253, visualizer.py 157, girok.py 76, wiki_api.py 206. khub package ~1500 LOC, tests ~700 LOC.
- TDD 디스크립린 유지: 7 task → 15 신규 테스트.
- cli.py split 임계값 권장: ~400 lines 또는 단일 command group >100 lines. capture group이 ~105로 1순위 split 후보 (`khub/cli/capture.py`).

## 미해결 / 별도 트랙 (Session 4 계승)
- Track-A: 진행자 에이전트 패턴 brainstorming
- Track-B: 기획군 직접 소통 회복 + CLAUDE.md 1급 격상
- Track-C: DnT 외 프로젝트 파이프라인 전환
- Track-D: 전문 에이전트 자체 서브 spawn

## 다음 세션 TODO (KH-004 Phase 4)
1. **세션 부트 검증**: 작업 디렉토리 `E:/KnowledgeHub/` (DnT_WesangGoon 아님), git HEAD `d488cb6`, `python -m pytest -q` → **69 passed** 재확인.
2. **Phase 4 (Task 22-26) 시작**: `/superpowers:subagent-driven-development` 재호출.
   - Task 22: Obsidian Vault 설정
   - Task 23: Smoke Test (`khub/smoke_test.py` 위치 — spec 변경됨)
   - Task 24: e2e 시나리오 테스트 (모션 스프링 튜닝)
   - Task 25: Hooks 통합 + 실행 가이드
   - Task 26: Phase 1 합격 기준 검증 (수동)
3. **Phase 3 backlog 우선 처리**: `/k-girok` wiring (Important #1) → Phase 4 진입 전에 spike로 처리할지 Phase 4 내부에 흡수할지 결정.
4. **MAINT-1**: CLAUDE.md TEMS 경로 정정 (`E:/AgentInterface/memory/tems_commit.py` → `E:/DnT/DnT_WesangGoon/memory/tems_commit.py`) — 여전히 미처리.

## 산출물
- 코드: `E:/KnowledgeHub/khub/{cli.py,girok.py,visualizer.py}` + 3 test files + 1 subagent def + 3 landing pages
- `handover_doc/2026-04-08_session5.md`
- `qmd_sessions/2026-04-08_session5.md`
