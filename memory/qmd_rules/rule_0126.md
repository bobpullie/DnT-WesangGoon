---
rule_id: 126
category: TGL
tags: project:all,domain:tems,source:session-s42,source:agent-auto,classification:TGL-P,abstraction:L2
trigger: detect_project_scope,filter_by_project,cwd fallback,case-insensitive,project 태그,preflight
severity: info
---

# [TGL] Rule #126

**Keywords:** detect_project_scope,filter_by_project,cwd fallback,case-insensitive,project 태그,preflight

**Rule:** [TGL-P / L2] Registry 부재 solo agent 의 프로젝트별 태그 필터 — cwd 경로 추출 + 대소문자 정규화
FORBIDDEN: detect_project_scope 가 Registry 없을 때 project:meta/all/빈문자열만 반환하는 단순 구현
REQUIRED: cwd 경로 세그먼트 소문자 추출 + agent_id 포함 + filter_by_project 에서 .lower() 비교

**Context:** registered-via-cli-v2

**Result:** pending
