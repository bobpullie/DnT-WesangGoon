---
rule_id: 83
category: TCL
tags: python,packaging,pytest,atlas,source:agent-auto
trigger: pyproject pythonpath namespace package flat layout import resolution load-bearing
severity: info
---

# [TCL] Rule #83

**Keywords:** pyproject pythonpath namespace package flat layout import resolution load-bearing

**Rule:** Python flat layout 스킬에서 테스트가 'from pkg.sub.mod' 형태로 import할 때 pyproject.toml의 [tool.pytest.ini_options] pythonpath=['..']로 해결. atlas/에 __init__.py 두지 말고 namespace package로. pythonpath 라인에 'Load-bearing' 주석 필수.

**Context:** auto-registered

**Result:** pending
