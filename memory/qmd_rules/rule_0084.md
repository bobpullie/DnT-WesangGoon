---
rule_id: 84
category: TGL
tags: atlas,topology,yaml,parser,hardening,source:agent-auto
trigger: frontmatter parser empty roundtrip leading newline accumulation bidirectional edit-in-place
severity: info
---

# [TGL] Rule #84

**Keywords:** frontmatter parser empty roundtrip leading newline accumulation bidirectional edit-in-place

**Rule:** Frontmatter parser 구현 시 두 가지 결함 주의. (1) 빈 frontmatter '---\n---\n...'가 'no frontmatter'로 파싱되는 버그 — 두 번째 '---\n' 검사를 명시적으로. (2) write→parse roundtrip이 body 앞에 '\n'을 누적하는 버그 — parse 시 leading '\n' 한 개 strip. Phase 1.6 bidirectional link edit-in-place처럼 roundtrip이 반복 발생하는 코드에서 fire한다.

**Context:** auto-registered

**Result:** pending
