---
rule_id: 103
category: TCL
tags: tems,phase3,impl,project:meta,source:agent-session32
trigger: phase3 enforcement tool_gate compliance_tracker decay pretooluse active_guards tool_pattern violation compliance
severity: info
---

# [TCL] Rule #103

**Keywords:** phase3 enforcement tool_gate compliance_tracker decay pretooluse active_guards tool_pattern violation compliance

**Rule:** TEMS Phase 3 (Enforcement) 완료. 추가된 파일: (1) memory/tool_gate_hook.py — PreToolUse matcher='', TGL-T tool_pattern 매칭 시 severity=critical는 deny JSON 차단, severity=info/warning은 <tgl-tool-alert> 경고. (2) memory/compliance_tracker.py — PostToolUse matcher='', active_guards.json 순회하여 tool_pattern/failure_signature/FORBIDDEN-keyword 중 하나라도 매칭되면 violation_count++, window(remaining_checks) 만료 시 위반 0건이면 compliance_count++. (3) memory/decay.py — 30/90일 자동 cold/archive 전환 CLI, Windows Task Scheduler/cron 예상 (수동 실행 가능). preflight_hook.record_fire가 TGL 발동 시 active_guards.json에 push하도록 확장. self-invocation 판정은 Bash 명령어가 hook 자체를 실행할 때만으로 제한 — Edit/Write file_path가 memory/*.py인 경우는 측정 대상에 포함.

**Context:** registered-via-cli-v2

**Result:** pending
