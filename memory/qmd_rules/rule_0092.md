---
rule_id: 92
category: TGL
tags: tems,architecture,migration,dependency,critical,source:session31-rollback
trigger: TEMS 표준화 마이그레이션 외부패키지 의존성 self-contained tems_core import 검증
severity: info
---

# [TGL] Rule #92

**Keywords:** TEMS 표준화 마이그레이션 외부패키지 의존성 self-contained tems_core import 검증

**Rule:** 위상군 자체 TEMS 코드를 외부 패키지(tems)로 마이그레이션할 때, 패키지가 site-packages에 실제로 설치/배포되어 있는지 import 검증을 사전에 수행하지 않으면 preflight_hook과 tems_commit이 모두 실패하여 메모리 시스템 전체가 무력화된다. 의존성 변경 전 'python -c "import {pkg}"' 검증 필수, 그리고 self-contained 백업(_backup_tier1)을 반드시 보존할 것

**Context:** auto-registered

**Result:** pending
