---
rule_id: 105
category: TCL
tags: tems,phase3,patch,audit,project:meta,source:agent-session32-p0p1patch
trigger: phase3 audit patch p0 p1 compliance_tracker tool_gate forbidden heuristic fallback dedup path normalization scope-aware mutating_tools observation
severity: info
---

# [TCL] Rule #105

**Keywords:** phase3 audit patch p0 p1 compliance_tracker tool_gate forbidden heuristic fallback dedup path normalization scope-aware mutating_tools observation

**Rule:** Phase 3 audit P0/P1 패치 완료: (P0) compliance_tracker.check_violation의 FORBIDDEN 키워드 휴리스틱을 fallback으로 한정 — tool_pattern/failure_signature가 둘 다 없는 guard + MUTATING_TOOLS(Edit/Write/Bash/NotebookEdit)일 때만 동작. distinct 토큰 dedup + FORBIDDEN_NOISE_TOKENS에 memory/hook/preflight/compliance 등 인프라 명사 추가. (P1-a) tool_gate_hook.record_active_guard에 rule_id 기준 dedup — 이미 active 시 window 리셋만. (P1-b) build_match_target에서 backslash → forward slash 정규화 — Windows 경로 자동 매칭. (P1-c) scope-aware remaining_checks 감소 — Read/Glob/Grep/Notebook 관찰형은 window를 건드리지 않음. 변형 도구만 compliance/violation window 대상. 스냅샷: memory/_post_S32_phase3_preP0_patch/.

**Context:** registered-via-cli-v2

**Result:** pending
