# TEMS runtime state (TGL#74)
# 복원: tems rebuild 또는 python -m tems.rebuild_from_qmd
memory/error_logs.db
memory/error_logs.db-journal
memory/error_logs.db-wal
memory/error_logs.db-shm
memory/_backup_tier1/
memory/_post_S29_standardization/
memory/_post_S32_phase3_preP0_patch/
memory/_post_S33_P1a_follow_patch/

# Phase 0 자기관찰 채널 — 머신별 런타임 로그
memory/tems_diagnostics.jsonl
memory/tool_failures.jsonl
memory/compliance_events.jsonl
memory/sdc_briefs.jsonl

# Phase 1 retrospective hook rate limit marker
memory/.retrospective_last_run

# Phase 3 runtime state (TGL guard window)
memory/active_guards.json

# DVC runtime logs (per-machine)
logs/checklist/

# Python
__pycache__/
*.pyc
*.pyo

# Environment
.env

# Claude Code local settings (machine-specific paths/hooks)
.claude/settings.local.json

# Backups (machine-local)
.backup/
*.bak

# OS
Thumbs.db
.DS_Store
$PROFILE

# Plugin installs (independent git repos with their own upstream)
# - converted to clones in S35; tracked by their own repo (bobpullie/DVC etc.)
.claude/skills/dvc/

# Scratch / working copies of other agent repos (S42: bobpullie/TEMS inspection)
.scratch_tems_work/
.scratch_tems_inspect/

# Runtime logs (session_end_sync 등)
logs/session_end_sync.log
logs/*.log
viewer/_launch.log
