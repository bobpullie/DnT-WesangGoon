# TEMS Tier 2 S3 — Registry + Scaffold + Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TEMS 코어를 공유 라이브러리로 추출하고, scaffold 스크립트와 `/TEMS:project` 글로벌 스킬을 만들어, 모든 에이전트가 동일한 TEMS 인프라를 사용하도록 한다.

**Architecture:** 위상군 전용 코드(`fts5_memory.py`, `tems_engine.py`)를 `E:/AgentInterface/tems_core/`로 이동하고 하드코딩을 파라미터화. 진입점(`preflight_hook.py`, `tems_commit.py`)은 범용 템플릿으로 `tems_templates/`에 보관. `tems_scaffold.py`가 결정론적으로 에이전트 환경을 구축하고, `~/.claude/skills/tems-project.md` 스킬이 이를 오케스트레이션.

**Tech Stack:** Python 3.11+, SQLite FTS5, QMD CLI, Claude Code Skills/Hooks

**Spec:** `docs/superpowers/specs/2026-04-09-tems-tier2-s3-design.md`

---

## File Structure

### New Files
- `E:/AgentInterface/tems_core/__init__.py` — 빈 패키지 마커
- `E:/AgentInterface/tems_core/fts5_memory.py` — 공유 코어 (위상군에서 이동 + 파라미터화)
- `E:/AgentInterface/tems_core/tems_engine.py` — 공유 코어 (위상군에서 이동 + 파라미터화)
- `E:/AgentInterface/tems_templates/preflight_hook.py` — 범용 진입점 템플릿
- `E:/AgentInterface/tems_templates/tems_commit.py` — 범용 진입점 템플릿
- `E:/AgentInterface/tems_scaffold.py` — scaffold 스크립트
- `E:/AgentInterface/tems_registry.json` — 중앙 레지스트리
- `C:/Users/bluei/.claude/skills/tems-project.md` — 글로벌 스킬
- `E:/DnT/DnT_WesangGoon/.claude/tems_agent_id` — 위상군 마커
- `E:/DnT/DnT_WesangGoon/memory/_backup_tier1/` — 백업 디렉토리

### Modified Files
- `E:/DnT/DnT_WesangGoon/memory/preflight_hook.py` — 템플릿 기반으로 교체
- `E:/DnT/DnT_WesangGoon/memory/tems_commit.py` — 템플릿 기반으로 교체

### Deleted Files (이동)
- `E:/DnT/DnT_WesangGoon/memory/fts5_memory.py` → 코어로 이동 (백업 보존)
- `E:/DnT/DnT_WesangGoon/memory/tems_engine.py` → 코어로 이동 (백업 보존)

---

### Task 1: tems_core/fts5_memory.py — 공유 코어 (Phase A)

**Files:**
- Create: `E:/AgentInterface/tems_core/__init__.py`
- Create: `E:/AgentInterface/tems_core/fts5_memory.py`
- Reference: `E:/DnT/DnT_WesangGoon/memory/fts5_memory.py` (원본)

- [ ] **Step 1: 디렉토리 생성**

```bash
mkdir -p "E:/AgentInterface/tems_core"
```

- [ ] **Step 2: `__init__.py` 생성**

```python
# E:/AgentInterface/tems_core/__init__.py
# TEMS Core — 공유 라이브러리 패키지 마커
```

- [ ] **Step 3: fts5_memory.py를 코어에 복사 + 파라미터화**

`E:/DnT/DnT_WesangGoon/memory/fts5_memory.py` 전체를 `E:/AgentInterface/tems_core/fts5_memory.py`에 복사하되, 다음 변경 적용:

**변경 1 — DB_PATH 하드코딩 제거:**
```python
# Before (line 28):
DB_PATH = Path(__file__).parent / "error_logs.db"

# After:
DB_PATH = None  # 외부에서 주입, None이면 호출자 책임
```

**변경 2 — MemoryDB 생성자 기본값 제거:**
```python
# Before (line 32):
class MemoryDB:
    def __init__(self, db_path: Optional[str] = None):
        self.db_path = Path(db_path) if db_path else DB_PATH

# After:
class MemoryDB:
    def __init__(self, db_path: str):
        self.db_path = Path(db_path)
        self.db_path.parent.mkdir(parents=True, exist_ok=True)
        self._init_db()
```

`db_path`를 필수 인자로 변경. 코어에서는 "어디에 DB가 있는지"를 호출자가 항상 명시해야 함.

나머지 로직(`_init_db`, `_conn`, `commit_memory`, `search`, `preflight`, `commit_tcl`, `commit_tgl`, `get_active_tcl`, `get_recent` 등)은 전부 그대로.

- [ ] **Step 4: 코어 단독 import 테스트**

```bash
python -c "import sys; sys.path.insert(0, 'E:/AgentInterface'); from tems_core.fts5_memory import MemoryDB; print('OK')"
```

Expected: `OK`

- [ ] **Step 5: 코어 MemoryDB 인스턴스 생성 테스트**

```bash
python -c "
import sys, tempfile, os
sys.path.insert(0, 'E:/AgentInterface')
from tems_core.fts5_memory import MemoryDB
tmp = os.path.join(tempfile.mkdtemp(), 'test.db')
db = MemoryDB(db_path=tmp)
print('Tables OK')
os.unlink(tmp)
"
```

Expected: `Tables OK`

---

### Task 2: tems_core/tems_engine.py — 공유 코어 (Phase A)

**Files:**
- Create: `E:/AgentInterface/tems_core/tems_engine.py`
- Reference: `E:/DnT/DnT_WesangGoon/memory/tems_engine.py` (원본)

- [ ] **Step 1: tems_engine.py를 코어에 복사 + 파라미터화**

`E:/DnT/DnT_WesangGoon/memory/tems_engine.py` 전체를 `E:/AgentInterface/tems_core/tems_engine.py`에 복사하되, 다음 변경 적용:

**변경 1 — import 경로:**
```python
# Before:
from memory.fts5_memory import MemoryDB

# After:
from fts5_memory import MemoryDB
```

**변경 2 — DB_PATH, QMD_RULES_DIR 하드코딩 제거:**
```python
# Before (line 28-29):
DB_PATH = Path(__file__).parent / "error_logs.db"
QMD_RULES_DIR = Path(__file__).parent / "qmd_rules"

# After:
DB_PATH = None  # 외부에서 주입
QMD_RULES_DIR = None  # 외부에서 주입
```

**변경 3 — HybridRetriever QMD 컬렉션 파라미터화:**
```python
# Before:
class HybridRetriever:
    RRF_K = 60
    def __init__(self, db: Optional[MemoryDB] = None):
        self.db = db or MemoryDB()

# After:
class HybridRetriever:
    RRF_K = 60
    def __init__(self, db: MemoryDB, collection: str = "tems-wesanggoon"):
        self.db = db
        self.collection = collection
```

`db`를 필수 인자로 변경 (코어에서 기본 MemoryDB() 생성 불가).

**변경 4 — `_dense_search`의 하드코딩된 컬렉션명:**
```python
# Before (line 103):
[QMD_CMD, "vsearch", query, "-c", "tems-wesanggoon", "--json"],

# After:
[QMD_CMD, "vsearch", query, "-c", self.collection, "--json"],
```

**변경 5 — 모든 클래스의 `db` 파라미터 필수화:**

`HealthScorer`, `AnomalyCertifier`, `MetaRuleEngine`, `RuleGraph`, `PredictiveTGL`, `AdaptiveTrigger`, `EnhancedPreflight` — 모두 동일 패턴 적용:

```python
# Before:
def __init__(self, db: Optional[MemoryDB] = None):
    self.db = db or MemoryDB()

# After:
def __init__(self, db: MemoryDB):
    self.db = db
```

**변경 6 — `sync_rules_to_qmd`, `sync_single_rule_to_qmd` 파라미터화:**
```python
# Before:
def sync_rules_to_qmd(db: Optional[MemoryDB] = None) -> int:
    db = db or MemoryDB()
    ...
    QMD_RULES_DIR.mkdir(...)

# After:
def sync_rules_to_qmd(db: MemoryDB, qmd_rules_dir: Path) -> int:
    qmd_rules_dir.mkdir(parents=True, exist_ok=True)
    ...
    # QMD_RULES_DIR → qmd_rules_dir 로 전부 교체

def sync_single_rule_to_qmd(rule_id: int, db: MemoryDB, qmd_rules_dir: Path):
    qmd_rules_dir.mkdir(parents=True, exist_ok=True)
    ...
```

나머지 모든 로직(RRF, THS, TAC, MetaRule, RuleGraph, PredictiveTGL, AdaptiveTrigger, EnhancedPreflight)은 그대로.

- [ ] **Step 2: 코어 tems_engine import 테스트**

```bash
python -c "
import sys
sys.path.insert(0, 'E:/AgentInterface')
from tems_core.tems_engine import HybridRetriever, HealthScorer, RuleGraph
print('All imports OK')
"
```

Expected: `All imports OK`

- [ ] **Step 3: HybridRetriever 인스턴스 생성 테스트**

```bash
python -c "
import sys, tempfile, os
sys.path.insert(0, 'E:/AgentInterface')
from tems_core.fts5_memory import MemoryDB
from tems_core.tems_engine import HybridRetriever
tmp = os.path.join(tempfile.mkdtemp(), 'test.db')
db = MemoryDB(db_path=tmp)
hr = HybridRetriever(db=db, collection='tems-testgoon')
print(f'Collection: {hr.collection}')
os.unlink(tmp)
"
```

Expected: `Collection: tems-testgoon`

---

### Task 3: tems_templates/ — 범용 진입점 (Phase B)

**Files:**
- Create: `E:/AgentInterface/tems_templates/preflight_hook.py`
- Create: `E:/AgentInterface/tems_templates/tems_commit.py`

- [ ] **Step 1: 디렉토리 생성**

```bash
mkdir -p "E:/AgentInterface/tems_templates"
```

- [ ] **Step 2: preflight_hook.py 템플릿 작성**

위상군의 `E:/DnT/DnT_WesangGoon/memory/preflight_hook.py` 전체를 복사하되, 다음 변경 적용:

**변경 1 — import 섹션 교체 (line 1~22):**
```python
"""
TEMS Preflight Hook — UserPromptSubmit 자동 트리거 (범용 템플릿)
================================================================
에이전트의 .claude/tems_agent_id 마커 파일을 기반으로 DB를 찾고,
tems_core 공유 라이브러리를 사용하여 preflight 검색을 수행합니다.
"""

import sys
import json
import re
from pathlib import Path

# tems_core 공유 라이브러리 로드
TEMS_CORE = Path("E:/AgentInterface")
sys.path.insert(0, str(TEMS_CORE))

from tems_core.fts5_memory import MemoryDB
from tems_core.tems_engine import EnhancedPreflight, RuleGraph, HybridRetriever


def find_agent_root(start: Path) -> Path:
    """상위 순회하며 .claude/tems_agent_id 찾기 (.git 탐색과 동일 패턴)"""
    cur = start.resolve()
    while cur != cur.parent:
        marker = cur / ".claude" / "tems_agent_id"
        if marker.exists():
            return cur
        cur = cur.parent
    raise FileNotFoundError("tems_agent_id not found from " + str(start))


# 에이전트 자기 식별
AGENT_ROOT = find_agent_root(Path(__file__).parent)
AGENT_ID = (AGENT_ROOT / ".claude" / "tems_agent_id").read_text(encoding="utf-8").strip()
DB_PATH = AGENT_ROOT / "memory" / "error_logs.db"
REGISTRY_PATH = Path("E:/AgentInterface/tems_registry.json")
```

**변경 2 — `detect_project_scope` 교체 (기존 하드코딩 → 레지스트리 기반):**
```python
def detect_project_scope(agent_id: str) -> list[str]:
    """tems_registry.json에서 에이전트의 프로젝트 조회"""
    scopes = ["project:meta", "project:all", ""]
    try:
        registry = json.loads(REGISTRY_PATH.read_text(encoding="utf-8"))
        projects = registry.get("agents", {}).get(agent_id, {}).get("projects", [])
        for p in projects:
            scopes.append(f"project:{p.lower()}")
    except (FileNotFoundError, json.JSONDecodeError):
        pass  # 레지스트리 없으면 기본 스코프만 사용
    return scopes
```

**변경 3 — `main()` 함수에서 MemoryDB 인스턴스 생성 변경:**
```python
# Before:
db = MemoryDB()

# After:
db = MemoryDB(db_path=str(DB_PATH))
```

**변경 4 — HybridRetriever 호출 변경:**
```python
# Before:
hybrid = HybridRetriever(db)

# After:
hybrid = HybridRetriever(db=db, collection=f"tems-{AGENT_ID}")
```

**변경 5 — `detect_project_scope` 호출 변경:**
```python
# Before:
allowed_scopes = detect_project_scope(cwd)

# After:
allowed_scopes = detect_project_scope(AGENT_ID)
```

나머지 로직(`strip_korean_suffix`, `extract_keywords`, context budget 상수, `get_ths_scores`, `rank_by_ths`, `format_rules`, `filter_by_project`, `detect_rule_intent` 등)은 전부 그대로.

단, `get_ths_scores()`에서 DB_PATH 참조를 모듈 변수 사용으로 통일:
```python
def get_ths_scores() -> dict[int, tuple[float, str]]:
    import sqlite3
    try:
        conn = sqlite3.connect(str(DB_PATH))
        conn.row_factory = sqlite3.Row
        rows = conn.execute("SELECT rule_id, ths_score, status FROM rule_health").fetchall()
        conn.close()
        return {r["rule_id"]: (r["ths_score"] or 0.5, r["status"] or "warm") for r in rows}
    except Exception:
        return {}
```

- [ ] **Step 3: tems_commit.py 템플릿 작성**

위상군의 `E:/DnT/DnT_WesangGoon/memory/tems_commit.py` 전체를 복사하되:

**변경 1 — import + DB_PATH 교체:**
```python
"""
TEMS 규칙 등록 CLI — 범용 템플릿
"""

import argparse
import sqlite3
import os
import sys
import json
from datetime import datetime
from pathlib import Path

# tems_core 공유 라이브러리 로드
TEMS_CORE = Path("E:/AgentInterface")
sys.path.insert(0, str(TEMS_CORE))


def find_agent_root(start: Path) -> Path:
    """상위 순회하며 .claude/tems_agent_id 찾기"""
    cur = start.resolve()
    while cur != cur.parent:
        marker = cur / ".claude" / "tems_agent_id"
        if marker.exists():
            return cur
        cur = cur.parent
    raise FileNotFoundError("tems_agent_id not found from " + str(start))


AGENT_ROOT = find_agent_root(Path(__file__).parent)
AGENT_ID = (AGENT_ROOT / ".claude" / "tems_agent_id").read_text(encoding="utf-8").strip()
DB_PATH = str(AGENT_ROOT / "memory" / "error_logs.db")
QMD_RULES_DIR = AGENT_ROOT / "memory" / "qmd_rules"
```

**변경 2 — `sync_single_rule_to_qmd` import 변경:**
```python
# Before:
from tems_engine import sync_single_rule_to_qmd

# After:
from tems_core.tems_engine import sync_single_rule_to_qmd
```

**변경 3 — `sync_single_rule_to_qmd` 호출에 db, qmd_rules_dir 전달:**
```python
# Before:
sync_single_rule_to_qmd(rule_id)

# After:
from tems_core.fts5_memory import MemoryDB
db = MemoryDB(db_path=DB_PATH)
sync_single_rule_to_qmd(rule_id, db=db, qmd_rules_dir=QMD_RULES_DIR)
```

나머지 `commit_rule`, `main` 로직은 그대로.

- [ ] **Step 4: 템플릿 syntax 검증**

```bash
python -c "import py_compile; py_compile.compile('E:/AgentInterface/tems_templates/preflight_hook.py', doraise=True); print('preflight OK')"
python -c "import py_compile; py_compile.compile('E:/AgentInterface/tems_templates/tems_commit.py', doraise=True); print('commit OK')"
```

Expected: `preflight OK`, `commit OK`

---

### Task 4: tems_scaffold.py — Scaffold 스크립트 (Phase B)

**Files:**
- Create: `E:/AgentInterface/tems_scaffold.py`

- [ ] **Step 1: scaffold 스크립트 작성**

```python
"""
TEMS Agent Scaffold — 결정론적 에이전트 환경 구축 스크립트

Usage:
  python tems_scaffold.py --agent-id realgoon --agent-name "리얼군" --project MysticIsland --cwd "E:/00_unrealAgent"
  python tems_scaffold.py --agent-id realgoon --agent-name "리얼군" --project MysticIsland --cwd "E:/00_unrealAgent" --force
"""

import argparse
import json
import shutil
import sqlite3
import sys
from datetime import datetime
from pathlib import Path

REGISTRY_PATH = Path("E:/AgentInterface/tems_registry.json")
TEMPLATES_DIR = Path("E:/AgentInterface/tems_templates")


def create_marker(cwd: Path, agent_id: str, force: bool) -> str:
    """Step 1: .claude/tems_agent_id 마커 파일 생성"""
    marker_dir = cwd / ".claude"
    marker_dir.mkdir(parents=True, exist_ok=True)
    marker = marker_dir / "tems_agent_id"

    if marker.exists() and not force:
        existing = marker.read_text(encoding="utf-8").strip()
        if existing == agent_id:
            return "marker_exists"
        raise ValueError(f"Marker exists with different ID: {existing} (use --force to overwrite)")

    marker.write_text(agent_id, encoding="utf-8")
    return "marker_created"


def create_directories(cwd: Path) -> list[str]:
    """Step 2: memory/ + qmd_rules/ 디렉토리 생성"""
    actions = []
    memory_dir = cwd / "memory"
    qmd_dir = memory_dir / "qmd_rules"

    if not memory_dir.exists():
        memory_dir.mkdir(parents=True)
        actions.append("memory_dir_created")

    if not qmd_dir.exists():
        qmd_dir.mkdir(parents=True)
        actions.append("qmd_rules_dir_created")

    return actions


def create_database(cwd: Path, force: bool) -> str:
    """Step 3: error_logs.db 전체 스키마 생성"""
    db_path = cwd / "memory" / "error_logs.db"

    if db_path.exists() and not force:
        # 스키마 검증만 수행
        conn = sqlite3.connect(str(db_path))
        tables = [r[0] for r in conn.execute(
            "SELECT name FROM sqlite_master WHERE type='table'"
        ).fetchall()]
        conn.close()
        required = {"memory_logs", "rule_health", "exceptions", "meta_rules",
                     "rule_edges", "co_activations", "tgl_sequences", "trigger_misses", "rule_versions"}
        missing = required - set(tables)
        if missing:
            # 누락 테이블만 추가 생성
            _create_tables(str(db_path), only_missing=missing)
            return "db_schema_updated"
        return "db_exists"

    _create_tables(str(db_path))
    return "db_created"


def _create_tables(db_path: str, only_missing: set = None):
    """전체 TEMS 스키마 생성"""
    conn = sqlite3.connect(db_path)

    schemas = {
        "memory_logs": """
            CREATE TABLE IF NOT EXISTS memory_logs (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                timestamp TEXT NOT NULL,
                context_tags TEXT NOT NULL,
                keyword_trigger TEXT DEFAULT '',
                action_taken TEXT NOT NULL,
                result TEXT NOT NULL,
                correction_rule TEXT,
                category TEXT DEFAULT 'general',
                severity TEXT DEFAULT 'info',
                summary TEXT DEFAULT '',
                created_at TEXT DEFAULT (datetime('now'))
            )
        """,
        "rule_health": """
            CREATE TABLE IF NOT EXISTS rule_health (
                rule_id INTEGER PRIMARY KEY,
                activation_count INTEGER DEFAULT 0,
                correction_success INTEGER DEFAULT 0,
                correction_total INTEGER DEFAULT 0,
                modification_count INTEGER DEFAULT 0,
                last_activated TEXT,
                last_modified TEXT,
                status TEXT DEFAULT 'warm',
                status_changed_at TEXT DEFAULT (datetime('now')),
                ths_score REAL DEFAULT 0.5,
                ths_updated_at TEXT DEFAULT (datetime('now')),
                FOREIGN KEY (rule_id) REFERENCES memory_logs(id)
            )
        """,
        "exceptions": """
            CREATE TABLE IF NOT EXISTS exceptions (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                rule_id INTEGER,
                exception_type TEXT NOT NULL,
                description TEXT NOT NULL,
                occurrence_count INTEGER DEFAULT 1,
                persistence_score REAL DEFAULT 0.0,
                created_at TEXT DEFAULT (datetime('now')),
                last_seen TEXT DEFAULT (datetime('now')),
                status TEXT DEFAULT 'active',
                promoted_to_rule_id INTEGER,
                FOREIGN KEY (rule_id) REFERENCES memory_logs(id)
            )
        """,
        "meta_rules": """
            CREATE TABLE IF NOT EXISTS meta_rules (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                level INTEGER NOT NULL,
                parameter_name TEXT NOT NULL,
                old_value REAL,
                new_value REAL,
                reason TEXT,
                system_health_before REAL,
                system_health_after REAL,
                created_at TEXT DEFAULT (datetime('now'))
            )
        """,
        "rule_edges": """
            CREATE TABLE IF NOT EXISTS rule_edges (
                rule_a INTEGER NOT NULL,
                rule_b INTEGER NOT NULL,
                edge_type TEXT NOT NULL,
                weight REAL DEFAULT 0.0,
                updated_at TEXT DEFAULT (datetime('now')),
                PRIMARY KEY (rule_a, rule_b, edge_type)
            )
        """,
        "co_activations": """
            CREATE TABLE IF NOT EXISTS co_activations (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                prompt_hash TEXT NOT NULL,
                rule_ids TEXT NOT NULL,
                created_at TEXT DEFAULT (datetime('now'))
            )
        """,
        "tgl_sequences": """
            CREATE TABLE IF NOT EXISTS tgl_sequences (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                predecessor_id INTEGER NOT NULL,
                successor_id INTEGER NOT NULL,
                occurrence_count INTEGER DEFAULT 1,
                confidence REAL DEFAULT 0.0,
                last_seen TEXT DEFAULT (datetime('now')),
                UNIQUE(predecessor_id, successor_id)
            )
        """,
        "trigger_misses": """
            CREATE TABLE IF NOT EXISTS trigger_misses (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                query TEXT NOT NULL,
                expected_rule_id INTEGER,
                timestamp TEXT DEFAULT (datetime('now'))
            )
        """,
        "rule_versions": """
            CREATE TABLE IF NOT EXISTS rule_versions (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                rule_id INTEGER NOT NULL,
                field_changed TEXT NOT NULL,
                old_value TEXT,
                new_value TEXT,
                timestamp TEXT DEFAULT (datetime('now')),
                FOREIGN KEY (rule_id) REFERENCES memory_logs(id)
            )
        """,
    }

    for name, ddl in schemas.items():
        if only_missing is None or name in only_missing:
            conn.execute(ddl)

    # FTS5 가상 테이블 (memory_logs 기반)
    if only_missing is None or "memory_fts" in only_missing:
        conn.execute("DROP TABLE IF EXISTS memory_fts")
        conn.execute("""
            CREATE VIRTUAL TABLE IF NOT EXISTS memory_fts USING fts5(
                context_tags, keyword_trigger, action_taken, result,
                correction_rule, category,
                content=memory_logs, content_rowid=id,
                tokenize='unicode61'
            )
        """)
        # 동기화 트리거
        conn.execute("""
            CREATE TRIGGER IF NOT EXISTS memory_ai AFTER INSERT ON memory_logs BEGIN
                INSERT INTO memory_fts(rowid, context_tags, keyword_trigger, action_taken, result, correction_rule, category)
                VALUES (new.id, new.context_tags, new.keyword_trigger, new.action_taken, new.result, new.correction_rule, new.category);
            END
        """)
        conn.execute("""
            CREATE TRIGGER IF NOT EXISTS memory_ad AFTER DELETE ON memory_logs BEGIN
                INSERT INTO memory_fts(memory_fts, rowid, context_tags, keyword_trigger, action_taken, result, correction_rule, category)
                VALUES ('delete', old.id, old.context_tags, old.keyword_trigger, old.action_taken, old.result, old.correction_rule, old.category);
            END
        """)
        conn.execute("""
            CREATE TRIGGER IF NOT EXISTS memory_au AFTER UPDATE ON memory_logs BEGIN
                INSERT INTO memory_fts(memory_fts, rowid, context_tags, keyword_trigger, action_taken, result, correction_rule, category)
                VALUES ('delete', old.id, old.context_tags, old.keyword_trigger, old.action_taken, old.result, old.correction_rule, old.category);
                INSERT INTO memory_fts(rowid, context_tags, keyword_trigger, action_taken, result, correction_rule, category)
                VALUES (new.id, new.context_tags, new.keyword_trigger, new.action_taken, new.result, new.correction_rule, new.category);
            END
        """)

    conn.commit()
    conn.close()


def copy_templates(cwd: Path, force: bool) -> list[str]:
    """Step 4: 진입점 템플릿 복사"""
    actions = []
    for filename in ("preflight_hook.py", "tems_commit.py"):
        src = TEMPLATES_DIR / filename
        dst = cwd / "memory" / filename
        if dst.exists() and not force:
            actions.append(f"{filename}_exists")
            continue
        shutil.copy2(str(src), str(dst))
        actions.append(f"{filename}_copied")
    return actions


def register_hook(cwd: Path) -> str:
    """Step 5: .claude/settings.local.json에 UserPromptSubmit hook 등록"""
    settings_path = cwd / ".claude" / "settings.local.json"

    hook_entry = {
        "type": "command",
        "command": f'python "{cwd}/memory/preflight_hook.py"'
    }

    if settings_path.exists():
        settings = json.loads(settings_path.read_text(encoding="utf-8"))
    else:
        settings = {}

    hooks = settings.setdefault("hooks", {})
    submit_hooks = hooks.setdefault("UserPromptSubmit", [])

    # 이미 등록된 preflight hook이 있는지 확인
    for existing in submit_hooks:
        if "preflight_hook.py" in existing.get("command", ""):
            # 경로 갱신
            existing["command"] = hook_entry["command"]
            settings_path.write_text(json.dumps(settings, indent=2, ensure_ascii=False), encoding="utf-8")
            return "hook_updated"

    submit_hooks.append(hook_entry)
    settings_path.write_text(json.dumps(settings, indent=2, ensure_ascii=False), encoding="utf-8")
    return "hook_registered"


def update_registry(agent_id: str, agent_name: str, project: str, db_path: str) -> str:
    """Step 6: tems_registry.json 갱신"""
    if REGISTRY_PATH.exists():
        registry = json.loads(REGISTRY_PATH.read_text(encoding="utf-8"))
    else:
        registry = {"version": 1, "registry_path": str(REGISTRY_PATH), "projects": {}, "agents": {}}

    now = datetime.now().strftime("%Y-%m-%d")

    # 프로젝트 등록
    if project not in registry["projects"]:
        registry["projects"][project] = {"aliases": [project], "status": "active"}

    # 에이전트 등록/갱신
    if agent_id in registry["agents"]:
        agent = registry["agents"][agent_id]
        if project not in agent.get("projects", []):
            agent.setdefault("projects", []).append(project)
        agent["db_path"] = db_path
        agent["status"] = "active"
        agent["last_verified"] = now
        action = "agent_updated"
    else:
        registry["agents"][agent_id] = {
            "name": agent_name,
            "projects": [project],
            "db_path": db_path,
            "status": "active",
            "last_verified": now,
        }
        action = "agent_registered"

    REGISTRY_PATH.write_text(json.dumps(registry, indent=2, ensure_ascii=False), encoding="utf-8")
    return action


def main():
    parser = argparse.ArgumentParser(description="TEMS Agent Scaffold")
    parser.add_argument("--agent-id", required=True, help="에이전트 고유 ID (예: realgoon)")
    parser.add_argument("--agent-name", required=True, help="에이전트 표시명 (예: 리얼군)")
    parser.add_argument("--project", required=True, help="프로젝트명 (예: MysticIsland)")
    parser.add_argument("--cwd", required=True, help="에이전트 작업 디렉토리")
    parser.add_argument("--force", action="store_true", help="기존 파일 덮어쓰기")
    args = parser.parse_args()

    cwd = Path(args.cwd).resolve()
    actions = []

    try:
        actions.append(create_marker(cwd, args.agent_id, args.force))
        actions.extend(create_directories(cwd))
        actions.append(create_database(cwd, args.force))
        actions.extend(copy_templates(cwd, args.force))
        actions.append(register_hook(cwd))
        db_path = str(cwd / "memory" / "error_logs.db")
        actions.append(update_registry(args.agent_id, args.agent_name, args.project, db_path))

        result = {"ok": True, "agent_id": args.agent_id, "actions": actions}
        print(json.dumps(result, ensure_ascii=False))
        sys.exit(0)

    except Exception as e:
        result = {"ok": False, "error": str(e), "actions": actions}
        print(json.dumps(result, ensure_ascii=False))
        sys.exit(1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: scaffold 테스트 — 임시 디렉토리에서 E2E**

```bash
python "E:/AgentInterface/tems_scaffold.py" --agent-id testgoon --agent-name "테스트군" --project TestProject --cwd "%TEMP%/tems_test_agent"
```

Expected JSON: `{"ok": true, "agent_id": "testgoon", "actions": [...]}`

- [ ] **Step 3: scaffold 결과 검증**

```bash
cat "%TEMP%/tems_test_agent/.claude/tems_agent_id"
```
Expected: `testgoon`

```bash
python -c "import sqlite3; conn = sqlite3.connect('%TEMP%/tems_test_agent/memory/error_logs.db'); print([r[0] for r in conn.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()])"
```
Expected: `['memory_logs', 'rule_health', 'exceptions', 'meta_rules', 'rule_edges', 'co_activations', 'tgl_sequences', 'trigger_misses', 'rule_versions']`

- [ ] **Step 4: 테스트 정리**

```bash
rm -rf "%TEMP%/tems_test_agent"
```

---

### Task 5: tems_registry.json — 초기 레지스트리 생성 (Phase D)

**Files:**
- Create: `E:/AgentInterface/tems_registry.json`

- [ ] **Step 1: 레지스트리 JSON 작성**

스펙 §4.2의 전체 에이전트 목록 기반. 위상군만 `last_verified` 설정:

```json
{
  "version": 1,
  "registry_path": "E:/AgentInterface/tems_registry.json",
  "projects": {
    "DnT": {"aliases": ["DnT", "MRV"], "status": "active"},
    "FermionQuant": {"aliases": ["FermionQuant", "Fermion"], "status": "active"},
    "MysticIsland": {"aliases": ["MysticIsland"], "status": "active"},
    "ChildSchedule": {"aliases": ["아이의하루", "ChildSchedule"], "status": "active"},
    "GCE": {"aliases": ["GCE"], "status": "active"},
    "TCAS": {"aliases": ["TCAS"], "status": "active"}
  },
  "agents": {
    "wesanggoon": {
      "name": "위상군",
      "projects": ["DnT"],
      "db_path": "E:/DnT/DnT_WesangGoon/memory/error_logs.db",
      "status": "active",
      "last_verified": "2026-04-09"
    },
    "buildgoon": {
      "name": "빌드군",
      "projects": ["DnT"],
      "db_path": "E:/MRV_DnT/memory/error_logs.db",
      "status": "active",
      "last_verified": null
    },
    "artgoon": {
      "name": "아트군",
      "projects": ["DnT"],
      "db_path": "E:/DnT/DnT_ArtGoon/memory/error_logs.db",
      "status": "active",
      "last_verified": null
    },
    "codegoon": {
      "name": "코드군",
      "projects": ["FermionQuant"],
      "db_path": "E:/QuantProject/DnT_Fermion/memory/error_logs.db",
      "status": "active",
      "last_verified": null
    },
    "jaemigoon": {
      "name": "재미군",
      "projects": ["FermionQuant"],
      "db_path": "E:/QuantProject/memory/error_logs.db",
      "status": "active",
      "last_verified": null
    },
    "gihakgoon": {
      "name": "기획군",
      "projects": ["FermionQuant"],
      "db_path": "E:/QuantProject/DNT_GihakGoon/memory/error_logs.db",
      "status": "active",
      "last_verified": null
    },
    "realgoon": {
      "name": "리얼군",
      "projects": ["MysticIsland"],
      "db_path": "E:/00_unrealAgent/memory/error_logs.db",
      "status": "active",
      "last_verified": null
    },
    "dinigoon": {
      "name": "디니군",
      "projects": ["MysticIsland"],
      "db_path": "E:/01_houdiniAgent/tems/tems_db.db",
      "status": "active",
      "last_verified": null
    },
    "appgoon": {
      "name": "어플군",
      "projects": ["ChildSchedule"],
      "db_path": "E:/ChildSchedule/memory/error_logs.db",
      "status": "active",
      "last_verified": null
    },
    "gaussgoon": {
      "name": "가우스군",
      "projects": ["GCE"],
      "db_path": "E:/GCE/GCE_Engineer/memory/error_logs.db",
      "status": "active",
      "last_verified": null
    },
    "managegoon": {
      "name": "관리군",
      "projects": ["TCAS"],
      "db_path": "E:/TCAS_BUILDER/memory/error_logs.db",
      "status": "active",
      "last_verified": null
    }
  }
}
```

- [ ] **Step 2: JSON 유효성 검증**

```bash
python -c "import json; json.load(open('E:/AgentInterface/tems_registry.json', encoding='utf-8')); print('Valid JSON')"
```

Expected: `Valid JSON`

---

### Task 6: 위상군 마이그레이션 (Phase C)

**Files:**
- Create: `E:/DnT/DnT_WesangGoon/memory/_backup_tier1/` (백업)
- Create: `E:/DnT/DnT_WesangGoon/.claude/tems_agent_id`
- Modify: `E:/DnT/DnT_WesangGoon/memory/preflight_hook.py` (템플릿으로 교체)
- Modify: `E:/DnT/DnT_WesangGoon/memory/tems_commit.py` (템플릿으로 교체)
- Delete: `E:/DnT/DnT_WesangGoon/memory/fts5_memory.py` (코어로 이동)
- Delete: `E:/DnT/DnT_WesangGoon/memory/tems_engine.py` (코어로 이동)

- [ ] **Step 1: 백업 생성**

```bash
mkdir -p "E:/DnT/DnT_WesangGoon/memory/_backup_tier1"
cp "E:/DnT/DnT_WesangGoon/memory/fts5_memory.py" "E:/DnT/DnT_WesangGoon/memory/_backup_tier1/"
cp "E:/DnT/DnT_WesangGoon/memory/tems_engine.py" "E:/DnT/DnT_WesangGoon/memory/_backup_tier1/"
cp "E:/DnT/DnT_WesangGoon/memory/preflight_hook.py" "E:/DnT/DnT_WesangGoon/memory/_backup_tier1/"
cp "E:/DnT/DnT_WesangGoon/memory/tems_commit.py" "E:/DnT/DnT_WesangGoon/memory/_backup_tier1/"
```

- [ ] **Step 2: 마커 파일 생성**

`.claude/tems_agent_id` 파일에 `wesanggoon` 기록:

```bash
echo -n "wesanggoon" > "E:/DnT/DnT_WesangGoon/.claude/tems_agent_id"
```

- [ ] **Step 3: 진입점을 템플릿으로 교체**

```bash
cp "E:/AgentInterface/tems_templates/preflight_hook.py" "E:/DnT/DnT_WesangGoon/memory/preflight_hook.py"
cp "E:/AgentInterface/tems_templates/tems_commit.py" "E:/DnT/DnT_WesangGoon/memory/tems_commit.py"
```

- [ ] **Step 4: 기존 코어 파일 삭제**

```bash
rm "E:/DnT/DnT_WesangGoon/memory/fts5_memory.py"
rm "E:/DnT/DnT_WesangGoon/memory/tems_engine.py"
```

- [ ] **Step 5: preflight hook 동작 테스트**

```bash
echo '{"prompt": "세션종료 퇴근", "cwd": "E:/DnT/DnT_WesangGoon"}' | python "E:/DnT/DnT_WesangGoon/memory/preflight_hook.py"
```

Expected: `<preflight-memory-check>` 태그가 출력되거나 (매칭 규칙 있을 때), 아무 출력 없이 종료 코드 0 (매칭 없을 때).

- [ ] **Step 6: tems_commit 동작 테스트**

```bash
python "E:/DnT/DnT_WesangGoon/memory/tems_commit.py" --type TCL --rule "마이그레이션 테스트 규칙 (삭제 예정)" --triggers "test migration" --tags "test" --json
```

Expected: `{"ok": true, "rule_id": ..., ...}` 또는 유사 규칙 존재 시 중복 에러.

테스트 규칙이 등록되었으면 DB에서 수동 삭제:
```bash
python -c "
import sqlite3
conn = sqlite3.connect('E:/DnT/DnT_WesangGoon/memory/error_logs.db')
conn.execute('DELETE FROM memory_logs WHERE correction_rule LIKE \"%마이그레이션 테스트%\"')
conn.commit()
print('Cleaned')
"
```

- [ ] **Step 7: 기존 테스트 실행**

```bash
cd "E:/DnT/DnT_WesangGoon" && python -m memory.tests.test_preflight_semantic
```

이 테스트가 `from memory.fts5_memory import`를 사용한다면 실패할 수 있음. 테스트 파일의 import도 tems_core 경로로 업데이트 필요. 실패 시 테스트 파일 수정 후 재실행.

- [ ] **Step 8: 실패 시 롤백 절차**

만약 Step 5~7에서 복구 불가능한 문제 발생 시:
```bash
cp "E:/DnT/DnT_WesangGoon/memory/_backup_tier1/fts5_memory.py" "E:/DnT/DnT_WesangGoon/memory/"
cp "E:/DnT/DnT_WesangGoon/memory/_backup_tier1/tems_engine.py" "E:/DnT/DnT_WesangGoon/memory/"
cp "E:/DnT/DnT_WesangGoon/memory/_backup_tier1/preflight_hook.py" "E:/DnT/DnT_WesangGoon/memory/"
cp "E:/DnT/DnT_WesangGoon/memory/_backup_tier1/tems_commit.py" "E:/DnT/DnT_WesangGoon/memory/"
```

---

### Task 7: /TEMS:project 글로벌 스킬 (Phase D)

**Files:**
- Create: `C:/Users/bluei/.claude/skills/tems-project.md`

- [ ] **Step 1: 스킬 마크다운 작성**

```markdown
---
name: TEMS:project
description: TEMS 에이전트 환경 scaffold + registry 관리. 신규 에이전트 온보딩, 레지스트리 현황 조회.
---

# /TEMS:project — TEMS 에이전트 환경 관리

## 사용법

- `/TEMS:project <프로젝트명>` — 현재 에이전트를 해당 프로젝트에 scaffold + register
- `/TEMS:project status` — 레지스트리 전체 현황 출력

## 실행 절차

### Scaffold (인자가 프로젝트명일 때)

1. **에이전트 식별**: `.claude/tems_agent_id` 파일을 현재 디렉토리부터 상위로 탐색
   - 있으면 → 기존 에이전트. 파일에서 agent-id 읽기
   - 없으면 → 신규 에이전트. 사용자에게 질문:
     - "에이전트 ID를 입력하세요 (영문 소문자, 예: realgoon)"
     - "에이전트 표시명을 입력하세요 (예: 리얼군)"

2. **scaffold 스크립트 실행**:
   ```bash
   python "E:/AgentInterface/tems_scaffold.py" \
     --agent-id <AGENT_ID> \
     --agent-name "<AGENT_NAME>" \
     --project <PROJECT_NAME> \
     --cwd "<현재 작업 디렉토리>"
   ```
   JSON 출력을 파싱하여 `ok` 필드 확인.

3. **사후 검증** (scaffold 성공 시):
   - `.claude/tems_agent_id` 읽기 → agent-id 일치 확인
   - `sqlite3 memory/error_logs.db ".tables"` → 테이블 존재 확인
   - `E:/AgentInterface/tems_registry.json`에서 해당 에이전트 항목 확인

4. **결과 리포트**:
   ```
   [TEMS] <agent_id> (<agent_name>) → <project> 프로젝트 온보딩 완료
   - DB: <db_path>
   - Preflight hook: 등록됨
   - Registry: active
   ```

### Status (인자가 "status"일 때)

1. `E:/AgentInterface/tems_registry.json` 읽기
2. 표 형식으로 출력:

| Agent ID | 이름 | 프로젝트 | Status | Last Verified |
|----------|------|---------|--------|---------------|
| ... | ... | ... | ... | ... |

에이전트별로 `db_path` 접근 가능 여부도 확인하여 stale 감지.
```

- [ ] **Step 2: 스킬 로드 확인**

Claude Code를 재시작하거나 새 세션에서 `/TEMS:project status` 입력 시 스킬이 인식되는지 확인.

---

### Task 8: 최종 통합 검증 (Phase D)

**Files:** 없음 (검증만)

- [ ] **Step 1: 위상군에서 `/TEMS:project DnT` 실행**

위상군 CWD에서 실행. 이미 scaffold 되어 있으므로 기존 항목 갱신 확인:
- `tems_registry.json`의 wesanggoon `last_verified`가 오늘 날짜로 갱신되는지 확인

- [ ] **Step 2: `/TEMS:project status` 실행**

레지스트리의 전체 에이전트 목록이 표로 출력되는지 확인.

- [ ] **Step 3: preflight hook 실동작 확인**

위상군에서 아무 프롬프트나 입력 → `<preflight-memory-check>` 태그가 주입되는지 확인.

- [ ] **Step 4: tems_commit 실동작 확인**

```bash
python "E:/DnT/DnT_WesangGoon/memory/tems_commit.py" --type TCL --rule "Tier2 S3 마이그레이션 완료 확인용 (삭제 예정)" --triggers "tier2 s3 migration" --tags "test,cleanup" --json
```

성공 후 테스트 규칙 삭제.

- [ ] **Step 5: 완료 확인 체크리스트**

- [ ] `E:/AgentInterface/tems_core/fts5_memory.py` 존재, import 성공
- [ ] `E:/AgentInterface/tems_core/tems_engine.py` 존재, import 성공
- [ ] `E:/AgentInterface/tems_templates/preflight_hook.py` 존재
- [ ] `E:/AgentInterface/tems_templates/tems_commit.py` 존재
- [ ] `E:/AgentInterface/tems_scaffold.py` 존재, `--help` 출력
- [ ] `E:/AgentInterface/tems_registry.json` 유효 JSON, 12개 에이전트
- [ ] `~/.claude/skills/tems-project.md` 존재
- [ ] `E:/DnT/DnT_WesangGoon/.claude/tems_agent_id` = "wesanggoon"
- [ ] `E:/DnT/DnT_WesangGoon/memory/_backup_tier1/` 4개 파일 보존
- [ ] 위상군 preflight hook 정상 작동
- [ ] 위상군 tems_commit 정상 작동
